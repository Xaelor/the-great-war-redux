ALB_historical_plan = {
	name = "International Control Commission"
	desc = "Albanian AI will side with the International Control Commission and attempt to crown a foreign monarch in order to stabilize and develop internal political institutions."
	allowed = {
		original_tag = ALB
	}
	enable = {
		original_tag = ALB
		OR = {
			has_game_rule = {
				rule = ALB_ai_behavior
				option = HISTORICAL_INTERNATIONAL_COMMISSION
			}
			has_country_flag = HISTORICAL_INTERNATIONAL_CONTROL_COMMISSION
			AND = {
				has_game_rule = {
					rule = ALB_ai_behavior
					option = DEFAULT
				}
				is_historical_focus_on = yes
			}
		}
		NOT = {
			MNT = {
				focus_progress = {
					focus = MNT_occupy_shkoder
					progress > 0
				}
			}
			count_triggers = {
				amount = 2
				num_of_controlled_states > 10
				TUR = {
					casualties_inflicted_by = {
						opponent = ALB
						thousands > 30
					}
				}
				has_country_flag = {
					flag = TUR_ongoing_rebellion_days
					days > 150
				}
				custom_trigger_tooltip = {
					tooltip = ALB_present_the_fourteen_demands_tt
					has_country_flag = {
						flag = TUR_ongoing_rebellion_days
						days > 150
					}
				}
			}
		}
	}
	abort = {
		original_tag = ALB
		any_neighbor_country = { tag = TUR }
		OR = {
			MNT = {
				focus_progress = {
					focus = MNT_occupy_shkoder
					progress > 0
				}
			}
			count_triggers = {
				amount = 2
				num_of_controlled_states > 10
				TUR = {
					casualties_inflicted_by = {
						opponent = ALB
						thousands > 30
					}
				}
				has_country_flag = {
					flag = TUR_ongoing_rebellion_days
					days > 150
				}
				custom_trigger_tooltip = {
					tooltip = ALB_present_the_fourteen_demands_tt
					has_country_flag = {
						flag = TUR_ongoing_rebellion_days
						days > 150
					}
				}
			}
		}
	}
	ai_national_focuses = {
		ALB_rilindja_kombetare
		ALB_sabotage_railway_passages
		ALB_guns_for_the_cause
		ALB_edith_durhams_expedition
		ALB_serbian_aid
		ALB_montenegrin_aid
		ALB_albanian_national_committee
		ALB_mobilize_the_rilindas
		ALB_black_hand_of_salvation
		ALB_tribal_allegiances
		ALB_establish_the_kimza_government
		ALB_international_weapon_aid
		ALB_local_support
		ALB_ensure_popular_support
		ALB_let_the_dominos_topple
		ALB_secure_regional_loyalties
		ALB_taksim_meeting
		ALB_present_the_fourteen_demands
		ALB_defending_our_homeland
		ALB_resist_weapon_confiscations
		ALB_utilize_the_kanun
		ALB_albanian_soldier_desertions
		ALB_knowing_your_enemy
		ALB_secure_bulgarian_support
		ALB_rulers_of_the_mountains
		ALB_weapon_scavenging
		ALB_financial_support_from_the_tosks
		ALB_asymmetric_warfare
		ALB_secure_the_vlora_triangle
		ALB_albanian_independence
		ALB_albanian_vilayet
		ALB_call_for_great_power_intervention
		ALB_albanian_national_army
	}
	# Keep small, as it is used as a factor for some things (such as research needs)
	# Recommended around 1.0. Useful for relation between plans
	weight = {
		factor = 1.0
		modifier = {
			MNT = {
				focus_progress = {
					focus = MNT_occupy_shkoder
					progress > 0
				}
			}
			factor = 0
		}
	}
}

ALB_draft_the_grece_memorandum_plan = {
	name = "Draft the Grece Memorandum"
	desc = "completes it when montenegro is about to take shkoder"
	allowed = {
		original_tag = ALB
	}
	enable = {
		original_tag = ALB
		any_neighbor_country = { tag = TUR }
		MNT = {
			focus_progress = {
				focus = MNT_occupy_shkoder
				progress > 0
			}
		}
	}
	ai_national_focuses = {
		ALB_draft_the_grece_memorandum
	}
	# Keep small, as it is used as a factor for some things (such as research needs)
	# Recommended around 1.0. Useful for relation between plans
	weight = {
		factor = 2
		modifier = {
			MNT = {
				focus_progress = {
					focus = MNT_occupy_shkoder
					progress > 0
				}
			}
			add = 3
		}
		modifier = {
			any_neighbor_country = { NOT = { tag = TUR } }
			factor = 0
		}
	}
}

ALB_present_the_fourteen_demands_plan = {
	name = "ALB_present_the_fourteen_demands"
	desc = "completes it when montenegro is about to take shkoder"
	allowed = {
		original_tag = ALB
	}
	enable = {
		original_tag = ALB
		any_neighbor_country = { tag = TUR }
		count_triggers = {
			amount = 2
			num_of_controlled_states > 10
			TUR = {
				casualties_inflicted_by = {
					opponent = ALB
					thousands > 30
				}
			}
			has_country_flag = {
				flag = TUR_ongoing_rebellion_days
				days > 150
			}
			custom_trigger_tooltip = {
				tooltip = ALB_present_the_fourteen_demands_tt
				has_country_flag = {
					flag = TUR_ongoing_rebellion_days
					days > 150
				}
			}
		}
	}
	ai_national_focuses = {
		ALB_present_the_fourteen_demands
	}
	# Keep small, as it is used as a factor for some things (such as research needs)
	# Recommended around 1.0. Useful for relation between plans
	weight = {
		factor = 2
		modifier = {
			any_neighbor_country = { NOT = { tag = TUR } }
			factor = 0
		}
		modifier = {
			OR = {
				num_of_controlled_states > 10
				TUR = {
					casualties_inflicted_by = {
						opponent = ALB
						thousands > 30
					}
				}
				has_country_flag = {
					flag = TUR_ongoing_rebellion_days
					days > 150
				}
				custom_trigger_tooltip = {
					tooltip = ALB_present_the_fourteen_demands_tt
					has_country_flag = {
						flag = TUR_ongoing_rebellion_days
						days > 150
					}
				}
			}
			add = 3
		}
	}
}
