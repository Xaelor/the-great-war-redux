
SWE_riksdagens_decisions = {
	# DEBUG
	SWE_debug_toggle_off = {
		allowed = { tag = SWE }
		visible = {
			has_country_flag = swe_debug_dec
		}
		complete_effect = {
			clr_country_flag = swe_debug_dec
		}
		ai_will_do = { factor = 0 }
	}
	SWE_debug_toggle_on = {
		allowed = { tag = SWE }
		visible = {
			NOT = { has_country_flag = swe_debug_dec }
		}
		complete_effect = {
			set_country_flag = swe_debug_dec
		}
		ai_will_do = { factor = 0 }
	}
	SWE_debug_first_chamber = {
		allowed = { tag = SWE }
		visible = {
			has_country_flag = swe_debug_dec
		}
		complete_effect = { SWE_recalculate_first_chamber = yes }
		ai_will_do = { factor = 0 }
	}
	SWE_debug_second_chamber = {
		allowed = { tag = SWE }
		visible = {
			has_country_flag = swe_debug_dec
		}
		complete_effect = { SWE_recalculate_second_chamber = yes }
		ai_will_do = { factor = 0 }
	}
	SWE_debug_first_chamber_info = {
		allowed = { tag = SWE }
		
		available = { always = no }
		visible = {
			has_country_flag = swe_debug_dec
		}
		ai_will_do = { factor = 0 }
	}
	SWE_debug_andra_chamber_info = {
		allowed = { tag = SWE }
		
		available = { always = no }
		visible = {
			has_country_flag = swe_debug_dec
		}
		ai_will_do = { factor = 0 }
	}
}
# NOT RIKSDAG BUT HERE CUZ MANY THINGS ARE RELATED TO RIKSDAG
SWE_political_issues = {
	SWE_conservative_agitation_against_karl_staaff = {
		allowed = {
			always = no
		}
		available = {
			OR = {
				has_defensive_war = yes
				AND = {
					has_global_flag = great_war
					democratic < 0.5
				}
				AND = {
					threat > 0.15
					power_balance_value = {
						id = SWE_parliamentalism_vs_royalism
						value < -0.15
					}
					has_idea = SWE_forsvarsvasendet
				}
				AND = {
					threat > 0.05
					neutrality > 0.2
					power_balance_value = {
						id = SWE_parliamentalism_vs_royalism
						value < -0.15
					}
					custom_trigger_tooltip = {
						tooltip = SWE_conservative_outrage_against_parliamentalism_tt
						OR = {
							date > 1914.2.1
							has_idea = SWE_forsvarsvasendet
						}
					}
				}
			}
		}
		cancel_trigger = {
			hidden_trigger = {
				OR = {
					has_completed_focus = SWE_third_liberal_regering
					has_completed_focus = SWE_courtyard_speech
				}
			}
		}
		
		fire_only_once = yes
		
		is_good = yes
		
		priority = 102
		
		days_mission_timeout = 1460
		
		complete_effect = {
			country_event = { id = ww1_sweden.4 }
			custom_effect_tooltip = karl_staaff_cant_be_re_elected
		}
		timeout_effect = {
			SWE_medium_riksdag_increase = yes
			custom_effect_tooltip = karl_staaff_can_be_re_elected
		}
	}
	SWE_retain_support_of_majority_of_riksdagen = {
		allowed = {
			always = no
		}
		available = {
			democratic < 0.5
			OR = {
				custom_trigger_tooltip = {
					tooltip = combined_soc_dem_support_is_below_fifty
					set_temp_variable = { leftwing_temp = party_popularity@democratic }
					add_to_temp_variable = { leftwing_temp = party_popularity@communism }
					NOT = {
						check_temp_variable = {
							var = leftwing_temp
							value = 0.5
						}
					}
				}
				AND = {
					custom_trigger_tooltip = {
						tooltip = majority_con_riksdag_f_tt
						set_temp_variable = { riksdag_f_temp = conservative_f_riksdag }
						check_temp_variable = {
							var = riksdag_f_temp
							value = 76
						}
					}
					custom_trigger_tooltip = {
						tooltip = majority_con_riksdag_a_tt
						set_temp_variable = { riksdag_a_temp = conservative_a_riksdag }
						check_temp_variable = {
							var = riksdag_a_temp
							value = 116
						}
					}
				}
			}
		}
		cancel_trigger = {
			OR = {
				has_completed_focus = SWE_third_liberal_regering
				has_completed_focus = SWE_courtyard_speech
			}
		}
		
		fire_only_once = yes
		
		is_good = yes
		
		priority = 100
		
		days_mission_timeout = 730
		
		complete_effect = {
			add_political_power = -50
			# Add event about this, this should be ahistorical
		}
		timeout_effect = {
			add_stability = 0.05
			SWE_tiny_riksdag_increase = yes
		}
	}
	SWE_address_prohibition_question = {
		allowed = {
			always = no
		}
		available = {
			has_completed_focus = SWE_prohibition_question
		}
		cancel_trigger = {
			OR = {
				has_completed_focus = SWE_third_liberal_regering
				has_completed_focus = SWE_courtyard_speech
			}
		}
		
		fire_only_once = yes
		
		days_mission_timeout = 500
		is_good = no
		
		priority = 100
		
		complete_effect = {
			add_political_power = 35
			if = {
				limit = { has_active_mission = SWE_rifts_in_liberal_party }
				add_days_mission_timeout = {
					mission = SWE_rifts_in_liberal_party
					days = 365
				}
			}
		}
		timeout_effect = {
			add_popularity = {
				ideology = democratic
				popularity = -0.01
			}
			add_to_variable = {
				tooltip = swe_lib_dem_vote_f_tt
				var = SWE_lib_dem_votes_f
				value = -0.02
			}
			add_to_variable = {
				tooltip = swe_agr_dem_vote_f_tt
				var = SWE_agr_dem_votes_f
				value = 0.02
			}
		}
	}
	SWE_anti_war_promises_mission = {
		allowed = {
			always = no
		}
		available = {
			OR = {
				threat > 0.3
				AND = {
					date > 1913.1.1
					threat > 0.10
				}
			}
		}
		cancel_trigger = {
			OR = {
				has_completed_focus = SWE_third_liberal_regering
				has_completed_focus = SWE_courtyard_speech
			}
		}
		
		fire_only_once = yes
		
		days_mission_timeout = 1095
		is_good = yes
		
		priority = 99
		
		complete_effect = {
			add_stability = -0.05
			add_war_support = 0.05
			add_timed_idea = {
				idea = SWE_rising_anti_war_concerns
				days = 365
			}
			country_event = { id = ww_sverige_riksdag.12 days = 7 }
		}
		timeout_effect = {
			add_political_power = 50
		}
	}
	SWE_public_demand_rearmament_mission = {
		allowed = {
			always = no
		}
		available = {
			has_completed_focus = SWE_swedish_rearmament
			has_completed_focus = SWE_support_the_military
		}
		cancel_trigger = {
			OR = {
				has_idea = idea_army_restrictions
				has_idea = idea_armistice
			}
		}
		
		fire_only_once = yes
		
		days_mission_timeout = 365
		priority = 100
		
		timeout_effect = {
			add_stability = -0.05
			add_political_power = -50
		}
	}
	SWE_solve_the_emigration_problem = {
		allowed = {
			always = no
		}
		available = {	# Unlikely, requires very early ww1 and other stuff
			NOT = {
				has_dynamic_modifier = { modifier = SWE_emigration_problem }
			}
		}
		
		fire_only_once = yes
		
		days_mission_timeout = 1825
		
		timeout_effect = {
			add_political_power = -150
		}
	}
	SWE_promised_of_reformed_workday = {
		allowed = {
			always = no
		}
		available = {
			has_country_flag = swe_eight_hour_workday
		}
		
		fire_only_once = yes
		
		days_mission_timeout = 365
		priority = 100
		
		complete_effect = {
			SWE_decrease_socialist_radicalisation = yes
			SWE_communist_tension_increase = yes
		}
		timeout_effect = {
			add_stability = -0.15
			add_political_power = -100
			swap_ideas = {
				add_idea = SWE_royal_dictatorship
				remove_idea = SWE_royal_dictatorship_mild
			}
		}
	}
	SWE_take_stance_in_foreign_politics_mission = {
		allowed = {
			always = no
		}
		available = {
			OR = {
				has_completed_focus = SWE_pro_entente_stance
				has_completed_focus = SWE_rally_to_neutrality
				has_completed_focus = SWE_strengthen_german_relations
			}
		}
		cancel_trigger = {
			has_war = yes
		}
		
		fire_only_once = yes
		
		days_mission_timeout = 90
		priority = 100
		
		complete_effect = {
			add_political_power = 25
		}
		timeout_effect = {
			add_war_support = -0.015
			activate_mission = SWE_take_stance_in_foreign_politics_mission
		}
	}
	SWE_the_threat_of_revolution = {
		allowed = {
			tag = SWE
		}
		activation = {
			date > 1913.1.1
			NOT = { has_country_flag = sweden_democratic_crisis }
			NOT = { has_country_flag = sweden_anti_demo_successful }
			OR = {
				NOT = { has_country_flag = SWE_suffrage_flag }
				AND = {
					OR = {
						NOT = { has_country_flag = swe_eight_hour_workday }
						NOT = {
							AND = {
								has_idea = SWE_parliamentalism
								has_government = democratic
								has_stability > 0.6
							}
						}
					}
				}
			}
			OR = {
				has_global_flag = october_revolution
				has_global_flag = british_civil_war
				has_global_flag = french_commune
				GER = { has_civil_war = yes }
				AND = {
					NOR = { has_government = communism }
					OR = {
						DEN = { has_government = communism }
						FIN = { has_government = communism }
					}
				}
			}
			OR = {
				has_country_flag = great_war_loser
				has_global_flag = interbellum
				AND = {	# Historical essentially
					GER = { has_country_flag = great_war_loser }
					has_global_flag = october_revolution
				}
			}
			NOT = {
				has_idea = SWE_civil_unrest
				has_country_flag = sweden_no_cw
				has_country_flag = sweden_civil_war
			}
		}
		available = {
			has_country_flag = SWE_suffrage_flag
			OR = {
				has_country_flag = swe_eight_hour_workday
				has_decision = SWE_the_eight_hour_workday	# Is passing it now, cuz time restraint otherwise stops this
				AND = {
					has_idea = SWE_parliamentalism
					has_government = democratic
					has_stability > 0.6
				}
			}
		}
		
		fire_only_once = yes
		
		days_mission_timeout = 140
		priority = 101
		
		complete_effect = {
			add_political_power = 35
			SWE_decrease_socialist_radicalisation = yes
			set_country_flag = sweden_no_cw
			SWE_communist_tension_increase = yes
		}
		timeout_effect = {
			SWE_increase_socialist_radicalisation = yes
			SWE_communist_tension_increase = yes
			add_stability = -0.10
			add_war_support = -0.20
			add_popularity = {
				ideology = communism
				popularity = 0.05
			}
			add_ideas = SWE_civil_unrest
			# Ultimatum given by left
				# Backing down gives democracy
				# Otherwise revolution!
			hidden_effect = {
				country_event = { id = ww_sverige_riksdag.14 days = 14 }	# Demands
				# Historical date - 13th-16th of May 1917
				country_event = { id = ww_sverige_riksdag.15 days = 90 random_days = 14 }	# SAP Split
			}
			set_country_flag = sweden_civil_war
		}
	}
	SWE_conservative_promises_of_suffrage = {
		allowed = {
			always = no
		}
		activation = {
			always = no
		}
		available = {
			has_country_flag = SWE_suffrage_flag
		}
		
		fire_only_once = yes
		
		days_mission_timeout = 90
		priority = 101
		
		complete_effect = {
			add_political_power = 35
		}
		timeout_effect = {
			add_stability = -0.05
			add_popularity = {
				ideology = communism
				popularity = 0.10
			}
			SWE_communist_tension_increase = yes
		}
	}
	SWE_the_civil_war_is_comming = {
		allowed = {
			always = no	# tag = SWE
			# TBA: later patch
		}
		activation = {
			always = no
		}
		available = {
			always = no
		}
		
		fire_only_once = yes
		
		days_mission_timeout = 30
		priority = 99
		
		timeout_effect = {
			# Civil War
		}
	}
	SWE_the_progressive_tax_question = {
		allowed = {
			always = no
		}
		
		available = {
			has_decision = SWE_implement_progressive_tax
		}
		
		fire_only_once = yes
		
		days_mission_timeout = 31
		priority = 10
		
		complete_effect = {
			# SocDem in charge - Bonus
				# Non-Socialist - Collapse
			add_popularity = {
				ideology = communism
				popularity = 0.03
			}
		}
		timeout_effect = {
			# Lib collapse of government
			add_political_power = -10
			if = {
				limit = {
					has_idea = SWE_liberal_social_coalition
				}
				custom_effect_tooltip = historical_tt
				custom_effect_tooltip = SWE_government_may_collapse_in_future
				hidden_effect = {
					country_event = { id = ww_sverige.19 days = 120 random_days = 20 }
				}
			}
		}
	}
	SWE_threat_of_a_republic = {
		allowed = {
			always = no	# tag = SWE
			# TBA: later patch
		}
		activation = {
			communism > 0.15
			OR = {
				AND = {
					has_idea = SWE_socialist_radicalisation_average
					has_stability < 0.55
				}
				has_idea = SWE_socialist_radicalisation_high
				has_idea = SWE_socialist_radicalisation_max
			}
			NOT = { has_country_flag = sweden_republican_mission }
			NOT = { has_active_mission = SWE_the_threat_of_revolution }
		}
		available = {
			SWE_has_majority_for_reform_republic = yes
		}
		
		fire_only_once = yes
		
		is_good = yes
		
		days_mission_timeout = 1825
		priority = 99
		
		complete_effect = {
			# Republic
				# Add event later
			set_country_flag = sweden_republican_mission
		}
		timeout_effect = {
			add_stability = 0.05
			set_country_flag = sweden_republican_mission
		}
	}
}
SWE_riksdagens_decisions = {
	# Missions
	SWE_emigrationsutredningen = {
		allowed = {
			always = no
		}
		available = {	# Unlikely, requires very early ww1 and other stuff
			NOT = {
				has_dynamic_modifier = { modifier = SWE_emigration_problem }
			}
		}
		
		fire_only_once = yes
		
		days_mission_timeout = 1095	# Get specific date later
		priority = 100
		
		timeout_effect = {
			custom_effect_tooltip = enables_further_riksdag_decisions
			# Social Reforms + Economic Reforms OR stricter emigration stopgaps in laws
				# Goal is just to reduce emigration mod to 0
			# Follow up mission to get rid of modifier
			activate_mission = SWE_solve_the_emigration_problem
		}
	}
	# Riksdag Slowdown
	SWE_riksdag_rigidness_removal = {	# Perhaps replaced with something else, but for now represents slowness to implement some reforms
		allowed = {
			always = no
		}
		available = {
			# If decision is used, then this will be removed
			has_political_power > 200
		}
		
		fire_only_once = yes
		
		is_good = yes
		
		selectable_mission = yes
		
		days_mission_timeout = 100
		complete_effect = {
			add_political_power = -200
			custom_effect_tooltip = restore_the_riksdag_requirement_to_base_value
			set_variable = {
				var = sweden_f_mandate_requirement
				value = 76
			}
			set_variable = {
				var = sweden_a_mandate_requirement
				value = 116
			}
		}
		timeout_effect = {
			custom_effect_tooltip = restore_the_riksdag_requirement_to_base_value
			set_variable = {
				var = sweden_f_mandate_requirement
				value = 76
			}
			set_variable = {
				var = sweden_a_mandate_requirement
				value = 116
			}
		}
		
		ai_will_do = {
			base = 0
			modifier = {
				add = 1
				has_active_mission = SWE_the_threat_of_revolution
			}
		}
	}
}
SWE_riksdagens_decisions = {
	# Decisions
	SWE_prepare_statskommisioner_for_wartime = {
		allowed = {
			original_tag = SWE
		}
		visible = {
			has_global_flag = great_war
			NOT = { has_global_flag = great_war_ends }
		}
		available = {
			NOT = { has_global_flag = great_war_ends }
		}
		
		cost = 100
		days_remove = -1
		fire_only_once = yes
		
		complete_effect = {
			add_stability = 0.03
		}
		modifier = {
			consumer_goods_factor = 0.10
			stability_factor = 0.05
		}
		
		ai_will_do = {
			base = 100
		}
	}
	SWE_end_statskommisioner = {
		allowed = {
			original_tag = SWE
		}
		visible = {
			has_decision = SWE_prepare_statskommisioner_for_wartime
		}
		available = {
			has_war = no
			has_global_flag = great_war_ends
			NOT = { has_country_flag = riksdag_decisions_are_blocked }
		}
		
		cost = 25
		
		complete_effect = {
			remove_decision = SWE_prepare_statskommisioner_for_wartime
		}
		
		ai_will_do = {
			base = 0
			modifier = {
				add = 10
				date > 1917.1.1
			}
		}
	}
	SWE_push_suffrage_through = {
		allowed = {
			original_tag = SWE
		}
		available = {
			OR = {
				has_government = democratic
				has_government = communism
				AND = {
					has_active_mission = SWE_the_threat_of_revolution
					NOT = { has_government = fascism }
				}
			}
			has_elections = yes
			custom_trigger_tooltip = {
				tooltip = conservatives_wont_support_this_unless_forced
				OR = {
					# Add more stuff later, but for now socialist takeover in major or civil wars/socialism in Nordic countries
					AND = {
						has_government = democratic
						DEN = {
							OR = {
								has_government = communism
								has_civil_war = yes
							}
						}
						NOR = {
							OR = {
								has_government = communism
								has_civil_war = yes
							}
						}
					}
					AND = {
						OR = {
							has_country_leader_ideology = social_liberalism
							has_country_leader_ideology = social_democracy
							has_government = communism
						}
						set_temp_variable = { leftwing_a_temp = liberal_a_riksdag }
						add_to_temp_variable = { leftwing_a_temp = socialist_a_riksdag }
						check_temp_variable = {
							var = leftwing_a_temp
							value = 116
						}
						set_temp_variable = { leftwing_f_temp = liberal_f_riksdag }
						add_to_temp_variable = { leftwing_f_temp = socialist_f_riksdag }
						check_temp_variable = {
							var = leftwing_f_temp
							value = 76
						}
						OR = {
							communism > 0.35
							democratic > 0.6
							date > 1917.1.1
						}
					}
					# For now (essentially historical situation)
					AND = {
						has_active_mission = SWE_the_threat_of_revolution
						NOT = { has_government = fascism }
						OR = {
							democratic > 0.15
							communism > 0.15
						}
					}
					AND = {
						has_active_mission = SWE_conservative_promises_of_suffrage
						has_government = democratic
					}
				}
			}
			NOT = { has_country_flag = riksdag_decisions_are_blocked }
		}
		fire_only_once = yes
		priority = 5
		
		days_remove = 7
		cost = 50
		
		complete_effect = {
			SWE_large_riksdag_increase = yes
			set_country_flag = SWE_suffrage_flag
			SWE_init_first_chamber = yes
			SWE_init_second_chamber = yes
			# Effect that gives tiny boost to con if conservative promises led to suffrage
		}
		remove_effect = {
			custom_effect_tooltip = adjust_swe_political_tt
			add_to_variable = {
				tooltip = political_power_cost_tt
				var = SWE_pol_political_power_cost
				value = -0.05
			}
			add_to_variable = {
				tooltip = stability_factor_tt
				var = SWE_pol_stability_factor
				value = 0.05
			}
			if = {
				limit = {
					check_variable = {
						var = SWE_pol_fascism_drift
						value = 0.01
					}
				}
				add_to_variable = {
					tooltip = fascism_drift_tt
					var = SWE_pol_fascism_drift
					value = -0.01
				}
			}
			custom_effect_tooltip = skip_line
			add_to_variable = {
				tooltip = swe_con_dem_vote_f_tt
				var = SWE_con_dem_votes_f
				value = -0.09
			}
			add_to_variable = {
				tooltip = swe_agr_dem_vote_f_tt
				var = SWE_agr_dem_votes_f
				value = -0.01
			}
			add_to_variable = {
				tooltip = swe_lib_dem_vote_f_tt
				var = SWE_lib_dem_votes_f
				value = -0.04
			}
			add_to_variable = {
				tooltip = swe_soc_dem_vote_f_tt
				var = SWE_soc_dem_votes_f
				value = 0.14
			}
			custom_effect_tooltip = skip_line
			add_to_variable = {
				tooltip = swe_lib_soc_vote_f_tt
				var = SWE_lib_soc_votes_f
				value = -0.24
			}
			add_to_variable = {
				tooltip = swe_soc_soc_vote_f_tt
				var = SWE_soc_soc_votes_f
				value = 0.18
			}
			add_to_variable = {
				tooltip = swe_com_soc_vote_f_tt
				var = SWE_com_soc_votes_f
				value = 0.06
			}
			hidden_effect = {
				country_event = { id = ww_sverige_general.58 days = 400 }
			}
			# Election should occur summer after if possible
		}
		ai_will_do = {
			base = 0.5
			modifier = {	# Suffrage important
				add = 5
				OR = {
					has_country_leader_ideology = social_liberalism
					has_country_leader_ideology = social_democracy
					has_government = communism
				}
			}
			modifier = {	# Try and avoid revolution
				add = 100
				OR = {
					has_active_mission = SWE_the_threat_of_revolution
					has_active_mission = SWE_conservative_promises_of_suffrage
				}
			}
			modifier = {	# Try after this date (temp)
				factor = 2
				date > 1917.1.1
			}
		}
	}
}
# Concession - Riksdag (TBA?)
SWE_riksdagens_decisions = {
	SWE_con_for_social_liberal_reform = {
		allowed = {
			tag = SWE
		}
		visible = {
			has_government = democratic
		}
		available = {
			has_political_power > 25	# to stop player from fucking self most of time
			OR = {
				has_country_leader_ideology = social_liberalism
				SWE_social_democracy_trigger = yes
			}
			custom_trigger_tooltip = {
				tooltip = SWE_concession_for_soc_lib_reform
				set_temp_variable = {
					var = leftwing_f
					value = socialist_f_riksdag
				}
				add_to_temp_variable = {
					var = leftwing_f
					value = liberal_f_riksdag
				}
				set_temp_variable = {
					var = leftwing_a
					value = socialist_a_riksdag
				}
				add_to_temp_variable = {
					var = leftwing_a
					value = liberal_a_riksdag
				}
				AND = {
					check_temp_variable = {
						var = lefwing_f
						value = 66
					}
					check_temp_variable = {
						var = lefwing_a
						value = 106
					}
				}
			}
		}
		
		fire_only_once = yes
		
		days_remove = 50
		days_re_enable = 200
		priority = 5
		
		modifier = {
			democratic_drift = -0.02
			neutrality_drift = 0.01
			fascism_drift = 0.01
		}
		
		complete_effect = {
			add_popularity = {
				ideology = democratic
				popularity = -0.02
			}
			custom_effect_tooltip = will_allow_easier_soc_lib_reforms_for_fifty_days
			set_country_flag = {
				flag = swe_soc_liberal_concession_flag
				days = 50
				value = 1
			}
		}
		
		ai_will_do = {
			base = 0
			modifier = {
				factor = 0
				has_political_power < 100
			}
		}
	}
	# Add decision to lose something short-term to win over some extra votes for:
		# Social-Liberal Reforms
			# Liberal/Socialists can do this to get +10 to the vote to bring it over edge
		# Liberal Reform
			# Liberals can do this to bring it over edge if enough conservatives
		# Reactionary Reform
			# Nationalist can do this to bring moderates into their reforms, at some heavy costs
	# These decisions are one-time per reform and should have a cooldown of one year
}
# Prohibition - Riksdag
SWE_riksdagens_decisions = {
	SWE_implement_motbokssystemet = {
		# motbokssystemet
		allowed = {
			tag = SWE
		}
		visible = {
			OR = {
				date > 1917.3.1
				AND = {
					date > 1916.12.1
					has_idea = SWE_stockholmssystemet
				}
			}
			NOT = { has_idea = prohibition_accepted }
			NOT = { has_variable = swe_proh_result }
		}
		available = {
			OR = {
				has_war = no
				AND = {
					has_war = yes
					has_war_support > 0.5
				}
			}
			OR = {
				AND = {
					has_decision = SWE_prepare_statskommisioner_for_wartime
					has_stability > 0.6
				}
				AND = {	# For now
					has_completed_focus = SWE_prohibition_question
					threat > 0.4
				}
				# 50% support for current party
			}
		}
		
		fire_only_once = yes
		
		days_remove = 30
		cost = 25
		
		remove_effect = {
			if = {
				limit = {
					NOT = { has_idea = SWE_stockholmssystemet }
				}
				add_ideas = SWE_motbokssystemet
			}
			else = {
				swap_ideas = {
					add_idea = SWE_motbokssystemet
					remove_idea = SWE_stockholmssystemet
				}
			}
		}
		
		ai_will_do = {
			base = 10
			modifier = {	# For now, might expand later
				factor = 0
				has_country_leader = {
					character = SWE_karl_staaff
					ruling_only = yes
				}
			}
		}
	}
	SWE_decide_on_prohibition = {
		# motbokssystemet
		# Nykterhetskommitté event first telling the decision is available
			# Two-part event
		# Possible mission popup to select this decision or something
		allowed = {
			tag = SWE
		}
		visible = {
			OR = {
				date > 1921.1.1
				AND = {
					date > 1920.1.1
					has_idea = SWE_motbokssystemet
				}
			}
		}
		available = {
			OR = {
				has_war = no
				AND = {
					has_war = yes
					has_war_support > 0.5
				}
			}
			OR = {
				AND = {
					has_decision = SWE_prepare_statskommisioner_for_wartime
					has_stability > 0.6
				}
				AND = {	# For now
					has_completed_focus = SWE_prohibition_question
					threat > 0.4
				}
				# 50% support for current party
			}
		}
		
		fire_only_once = yes
		
		days_remove = 35
		cost = 50
		
		remove_effect = {
			country_event = { id = ww_sverige_prohibition.1 }
			set_variable = {
				var = swe_proh_result
				value = 0
			}
		}
		
		ai_will_do = {
			base = 100
			modifier = {	# For now, might expand later
				factor = 0
				has_country_leader = {
					character = SWE_karl_staaff
					ruling_only = yes
				}
			}
		}
	}
	SWE_motboksystemet_decision = {
		allowed = {
			tag = SWE
		}
		visible = {
			has_idea = SWE_motbokssystemet
			has_variable = swe_proh_result
		}
		available = {
			SWE_has_majority_for_reform_prohibition = yes
		}
		
		fire_only_once = yes
		
		days_remove = 35
		cost = 35
		complete_effect = {
			clear_variable = swe_proh_result
		}
		
		remove_effect = {
			add_stability = 0.03
		}
		
		ai_will_do = {
			base = 0.1
			modifier = {
				factor = 10
				is_historical_focus_on = yes
			}
		}
	}
	SWE_lower_prohibition_decision = {
		allowed = {
			tag = SWE
		}
		visible = {
			has_variable = swe_proh_result
			NOT = {
				check_variable = {
					var = swe_proh_result
					value = 0.66
				}
			}
			OR = {
				has_idea = SWE_stockholmssystemet
				has_idea = SWE_motbokssystemet
			}
		}
		available = {
			SWE_has_majority_for_reform_prohibition = yes
		}
		
		fire_only_once = yes
		
		days_remove = 35
		cost = 50
		complete_effect = {
			clear_variable = swe_proh_result
		}
		
		remove_effect = {
			custom_effect_tooltip = adjust_swe_economic_tt
			add_to_variable = {
				tooltip = political_power_gain_tt
				var = SWE_eco_political_power_gain
				value = 0.05
			}
			add_to_variable = {
				tooltip = consumer_goods_factor_tt
				var = SWE_consumer_goods
				value = 0.025
			}
		}
		
		ai_will_do = {
			base = 0
			modifier = {
				set_temp_variable = {
					var = lower_amnt_calc
					value = 1
				}
				subtract_from_temp_variable = {
					var = lower_amnt_calc
					value = swe_proh_result
				}
				add = var:lower_amnt_calc
			}
			modifier = {
				add = 0.5
				political_power_daily < 0.5
			}
		}
	}
	SWE_higher_prohibition_decision = {
		allowed = {
			tag = SWE
		}
		visible = {
			has_variable = swe_proh_result
			check_variable = {
				var = swe_proh_result
				value = 0.45
			}
		}
		available = {
			SWE_has_majority_for_reform_prohibition_strong = yes
		}
		
		fire_only_once = yes
		
		days_remove = 35
		cost = 50
		complete_effect = {
			clear_variable = swe_proh_result
		}
		
		remove_effect = {
			custom_effect_tooltip = adjust_swe_economic_tt
			add_to_variable = {
				tooltip = political_power_gain_tt
				var = SWE_eco_political_power_gain
				value = -0.05
			}
			add_to_variable = {
				tooltip = consumer_goods_factor_tt
				var = SWE_consumer_goods
				value = -0.025
			}
		}
		
		ai_will_do = {
			base = 0
			modifier = {
				add = 1
				check_variable = {
					var = swe_proh_result
					value = 0.66
				}
			}
			modifier = {
				factor = 0.5
				political_power_daily < 1
			}
		}
	}
	SWE_full_prohibition_decision = {
		allowed = {
			tag = SWE
		}
		visible = {
			has_variable = swe_proh_result
			check_variable = {
				var = swe_proh_result
				value = 0.66
			}
		}
		available = {
			SWE_has_majority_for_reform_prohibition_strong = yes
			OR = {
				communism > 0.4
				fascism > 0.4
			}
		}
		
		fire_only_once = yes
		
		days_remove = 35
		cost = 75
		complete_effect = {
			clear_variable = swe_proh_result
			SWE_increase_riksdag_rigidity = yes
		}
		
		remove_effect = {
			custom_effect_tooltip = adjust_swe_economic_tt
			add_to_variable = {
				tooltip = political_power_gain_tt
				var = SWE_eco_political_power_gain
				value = -0.10
			}
			add_to_variable = {
				tooltip = consumer_goods_factor_tt
				var = SWE_consumer_goods
				value = -0.05
			}
			if = {
				limit = {
					has_idea = SWE_stockholmssystemet
				}
				swap_ideas = {
					add_idea = prohibition_accepted
					remove_idea = SWE_stockholmssystemet
				}
			}
			if = {
				limit = {
					has_idea = SWE_motbokssystemet
				}
				swap_ideas = {
					add_idea = prohibition_accepted
					remove_idea = SWE_motbokssystemet
				}
			}
			else = {
				add_ideas = prohibition_accepted
			}
		}
		
		ai_will_do = {
			base = 0
			modifier = {
				add = 2
				check_variable = {
					var = swe_proh_result
					value = 0.66
				}
			}
			modifier = {
				factor = 0.5
				political_power_daily < 1
			}
		}
	}
	SWE_move_past_decemberkompromissen = {
		# Decemberkompromissen isn't enough, we need to take further steps to fix the class conflict
			# After last strike many considered it broken by both sides
		allowed = {
			tag = SWE
		}
		visible = {
			has_completed_focus = SWE_socialliberal_reforms
		}
		
		available = {
			OR = {
				has_country_leader_ideology = social_liberalism
				SWE_social_democracy_trigger = yes
				has_government = communism
			}
			neutrality < 0.25
			fascism < 0.25
			SWE_has_majority_for_reform_social_liberal = yes
			NOT = { has_country_flag = riksdag_decisions_are_blocked }
		}
		
		fire_only_once = yes
		
		days_remove = 25
		cost = 75
		
		modifier = { political_power_cost = 1 }
		
		complete_effect = {
			SWE_increase_riksdag_rigidity = yes
			set_country_flag = swe_handled_class_conflict
		}
		
		remove_effect = {
			custom_effect_tooltip = adjust_swe_political_tt
			add_to_variable = {
				tooltip = political_power_factor_tt
				var = SWE_pol_political_power_factor
				value = 0.05
			}
			if = {
				limit = { NOT = { has_idea = SWE_class_conflict } }
				add_ideas = SWE_class_compromise
			}
			else = {
				swap_ideas = {
					add_idea = SWE_class_compromise
					remove_idea = SWE_class_conflict
				}
			}
			set_country_flag = swe_handled_class_conflict
			
			add_to_variable = {
				tooltip = swe_con_dem_vote_f_tt
				var = SWE_con_dem_votes_f
				value = -0.01
			}
			add_to_variable = {
				tooltip = swe_agr_dem_vote_f_tt
				var = SWE_agr_dem_votes_f
				value = -0.01
			}
			add_to_variable = {
				tooltip = swe_soc_dem_vote_f_tt
				var = SWE_soc_dem_votes_f
				value = 0.02
			}
		}
		
		ai_will_do = {
			base = 1
		}
	}
	SWE_establish_socialstyerlsen = {	#  Kungliga Socialstyrelsen 1912
		allowed = {
			tag = SWE
		}
		visible = {
			date > 1911.11.1
			NOT = { has_country_flag = swe_socialstyrelsen }
		}
		available = {
			SWE_has_majority_for_reform_social_liberal = yes
		}
		
		fire_only_once = yes
		
		cost = 25
		days_remove = 60
		
		complete_effect = {
			set_country_flag = swe_socialstyrelsen
		}
		
		remove_effect = {
			custom_effect_tooltip = adjust_swe_political_tt
			add_to_variable = {
				tooltip = stability_factor_tt
				var = SWE_pol_stability_factor
				value = 0.01
			}
			custom_effect_tooltip = adjust_emigration_problem_tt
			add_to_variable = {
				tooltip = monthly_population_tt
				var = SWE_emigration_monthly_population
				value = 0.02
			}
		}
		ai_will_do = {
			base = 10
			modifier = {
				factor = 10
				NOT = { has_completed_focus = SWE_extend_social_programs }
			}
		}
	}
	SWE_establish_veterinary_agency = {	# Statens veterinärbakteriologiska anstalt
		allowed = {
			tag = SWE
		}
		visible = {
			date > 1911.11.1
			SWE_arvid_bergman = {	# Simple check
				is_advisor = no
			}
		}
		available = {
			SWE_has_majority_for_reform_social_liberal = yes
		}
		
		fire_only_once = yes
		
		days_remove = 30
		cost = 10
		
		complete_effect = {
			add_stability = 0.01
		}
		
		remove_effect = {
			add_advisor_role = {
				character = SWE_arvid_bergman
				advisor = {
					slot = political_advisor
					cost = 75
					idea_token = SWE_arvid_bergman
					traits = {
						SWE_influential_doctor
					}
				}
			}
		}
		ai_will_do = {
			base = 100
		}
	}
}
# Riksdagen - Leftist
SWE_riksdagens_decisions = {
	# Behörighetslagen - Kerstin Hesselgren
	SWE_tax_the_well_off_to_fund_military = {
		# Historical 1913 tax hike
		allowed = {
			original_tag = SWE
		}
		visible = {
			OR = {
				date > 1912.12.1
				has_country_flag = SWE_rearm_easier
			}
		}
		available = {
			NOT = { has_idea = SWE_higher_taxes }
			date > 1913.1.1
			OR = {
				AND = {
					has_completed_focus = SWE_sensible_posturing
					has_country_leader = {
						character = SWE_karl_staaff
						ruling_only = yes
					}
				}
				has_completed_focus = SWE_fix_what_is_backwards
				has_completed_focus = SWE_promote_reasonable_reforms
				has_completed_focus = SWE_welfare_reforms
			}
			OR = {
				has_country_leader_ideology = social_liberalism
				SWE_social_democracy_trigger = yes
				has_government = communism
			}
			# Basically social liberal trigger without support from right
			OR = {
				threat > 0.15
				SWE_has_majority_for_reform_radical = yes
			}
		}
		
		fire_only_once = yes
		
		days_remove = 120
		
		cost = 100
		
		modifier = {
			political_power_cost = 0.25
		}
		
		complete_effect = {
			add_timed_idea = {
				idea = SWE_higher_taxes
				days = 365
			}
			SWE_increase_riksdag_rigidity = yes
		}
		
		remove_effect = {
			custom_effect_tooltip = adjust_swe_political_tt
			add_to_variable = {
				tooltip = stability_factor_tt
				var = SWE_pol_stability_factor
				value = -0.05
			}
			add_to_variable = {
				tooltip = defensive_war_stability_factor_tt
				var = SWE_pol_defensive_war_stability_factor
				value = 0.05
			}
			custom_effect_tooltip = adjust_swe_economic_tt
			add_to_variable = {
				tooltip = industrial_capacity_factory_tt
				var = SWE_mil_industrial_capacity_factory
				value = 0.02
			}
		}
		
		ai_will_do = {
			base = 10
			modifier = {
				add = 10
				date > 1913.2.1
			}
			modifier = {
				add = 10
				date > 1913.3.1
			}
			modifier = {
				add = 10
				date > 1913.4.1
			}
			modifier = {
				add = 10
				date > 1913.5.1
			}
			modifier = {
				add = 10
				date > 1913.6.1
			}
			modifier = {
				factor = 2
				has_war = yes
			}
		}
	}
	SWE_implement_allman_folkpension = {
		# People over 67, have some money
			# (less so if you're a woman unfortunately)
			# 150 kr/år if ur a man, 140 if ur a woman
				# average industry-person wage yearly at time was about 1300
		# Event about protests from Centralförbundet för socialt arbete
		allowed = {
			tag = SWE
		}
		visible = {
			date > 1911.1.1
		}
		available = {
			has_stability > 0.35
			SWE_has_majority_for_reform_social_liberal = yes
			NOT = { has_country_flag = riksdag_decisions_are_blocked }
		}
		fire_only_once = yes
		
		days_remove = 400
		cost = 25
		
		complete_effect = {
			hidden_effect = {
				country_event = { id = ww_sverige_general.0 days = 40 }
			}
			custom_effect_tooltip = adjust_emigration_problem_tt
			add_to_variable = {
				tooltip = monthly_population_tt
				var = SWE_emigration_monthly_population
				value = 0.01
			}
			SWE_remove_concession_boost = yes
		}
		
		remove_effect = {
			add_popularity = {
				ideology = democratic
				popularity = 0.01
			}
			add_popularity = {
				ideology = communism
				popularity = 0.01
			}
			custom_effect_tooltip = adjust_swe_political_tt
			add_to_variable = {
				tooltip = stability_factor_tt
				var = SWE_pol_stability_factor
				value = 0.01
			}
			custom_effect_tooltip = adjust_emigration_problem_tt
			add_to_variable = {
				tooltip = monthly_population_tt
				var = SWE_emigration_monthly_population
				value = 0.03
			}
		}
		ai_will_do = {
			base = 0
			modifier = {
				add = 100
				date > 1912.1.1
			}
		}
	}
	SWE_implement_militieombudsmannen = {
		# More civ overesight over military laws and such
		allowed = {
			tag = SWE
		}
		visible = {
			date > 1913.1.1
		}
		available = {
			democratic > 0.3
			OR = {
				has_country_leader_ideology = social_liberalism
				SWE_social_democracy_trigger = yes
				has_government = communism
			}
			SWE_has_majority_for_reform_radical = yes
		}
		fire_only_once = yes
		
		days_remove = 35
		cost = 25
		
		remove_effect = {
			custom_effect_tooltip = adjust_swe_political_tt
			add_to_variable = {
				tooltip = political_power_cost_tt
				var = SWE_pol_political_power_cost
				value = 0.05
			}
			if = {
				limit = {
					check_variable = {
						var = SWE_pol_neutrality_drift
						value = 0.01
					}
				}
				add_to_variable = {
					tooltip = neutrality_drift_tt
					var = SWE_pol_neutrality_drift
					value = -0.01
				}
			}
			custom_effect_tooltip = adjust_swe_military_tt
			add_to_variable = {
				tooltip = mobilization_laws_cost_factor_tt
				var = SWE_mil_mobilization_laws_cost_factor
				value = -0.05
			}
		}
		ai_will_do = {
			base = 0
			modifier = {
				add = 1
				date > 1915.1.1
			}
			modifier = {
				add = 1
				date > 1915.3.1
			}
			modifier = {
				factor = 2.5
				date > 1915.6.1
			}
			modifier = {
				factor = 0.5
				political_power_daily < 1
			}
		}
	}
	SWE_the_eight_hour_workday = {
		allowed = {
			original_tag = SWE
		}
		
		available = {
			if = {
				limit = { has_active_mission = SWE_the_threat_of_revolution }
				OR = {
					NOT = { has_government = fascism }
					has_stability < 0.5
				}
				OR = {
					has_completed_focus = SWE_reason_and_our_causes
					has_completed_focus = SWE_welfare_reforms
					communism > 0.1
				}
			}
			else = {
				OR = {
					AND = {
						has_country_leader_ideology = social_liberalism
						NOT = { has_idea = SWE_class_conflict }
					}
					AND = {
						has_completed_focus = SWE_reason_and_our_causes
						has_country_leader = {
							character = SWE_gustaf_v
							ruling_only = yes
						}
					}
					SWE_social_democracy_trigger = yes
					has_government = communism
				}
				OR = {
					has_completed_focus = SWE_reason_and_our_causes
					has_completed_focus = SWE_welfare_reforms
					communism > 0.35
				}
			}
			if = {
				limit = {
					OR = {
						has_completed_focus = SWE_reason_and_our_causes
						has_active_mission = SWE_the_threat_of_revolution
					}
				}
				SWE_has_majority_for_reform_social_liberal = yes
			}
			else = {
				SWE_has_majority_for_reform_radical = yes
			}
		}
		fire_only_once = yes
		
		days_remove = 151
		cost = 25
		
		modifier = { political_power_gain = -0.5 }
		
		complete_effect = {
			SWE_increase_riksdag_rigidity = yes
		}
		remove_effect = {
			#add_ideas = SWE_eight_hour_workday
			custom_effect_tooltip = adjust_swe_economic_tt
			add_to_variable = {
				tooltip = stability_factor_tt
				var = SWE_eco_stability
				value = 0.05
			}
			add_to_variable = {
				tooltip = production_factory_max_efficiency_factor_tt
				var = SWE_eco_production_max
				value = 0.03
			}
			add_to_variable = {
				tooltip = industrial_capacity_factory_tt
				var = SWE_mil_industrial_capacity_factory
				value = -0.05
			}
			add_to_variable = {
				tooltip = industrial_capacity_dockyard_tt
				var = SWE_mil_industrial_capacity_dockyard
				value = -0.05
			}
			custom_effect_tooltip = adjust_emigration_problem_tt
			add_to_variable = {
				tooltip = monthly_population_tt
				var = SWE_emigration_monthly_population
				value = 0.08
			}
			set_country_flag = swe_eight_hour_workday
			custom_effect_tooltip = skip_line
			SWE_decrease_socialist_radicalisation = yes
		}
		ai_will_do = {
			base = 100
			modifier = {
				factor = 0
				date < 1919.8.4
			}
		}
	}
	SWE_implement_progressive_tax = {
		allowed = {
			original_tag = SWE
		}
		visible = {
			date > 1914.1.1
			OR = {
				has_completed_focus = SWE_riksdagens_breakthrough
				has_completed_focus = SWE_welfare_reforms
			}
		}
		
		available = {
			OR = {
				has_completed_focus = SWE_riksdagens_breakthrough
				has_completed_focus = SWE_welfare_reforms
			}
			OR = {
				has_government = communism
				SWE_social_democracy_trigger = yes
				AND = {
					has_country_leader_ideology = social_liberalism
					has_completed_focus = SWE_welfare_reforms
				}
			}
			SWE_has_majority_for_reform_socialist = yes
		}
		fire_only_once = yes
		
		days_remove = 120
		cost = 75
		
		complete_effect = {
			SWE_increase_riksdag_rigidity = yes
		}
		remove_effect = {
			custom_effect_tooltip = adjust_swe_economic_tt
			add_to_variable = {
				tooltip = consumer_goods_factor_tt
				var = SWE_consumer_goods
				value = -0.05
			}
			custom_effect_tooltip = adjust_swe_political_tt
			add_to_variable = {
				tooltip = political_power_factor_tt
				var = SWE_pol_political_power_factor
				value = 0.05
			}
			add_to_variable = {
				tooltip = stability_factor_tt
				var = SWE_pol_stability_factor
				value = -0.10
			}
		}
		ai_will_do = {
			base = 1
			modifier = {
				add = 0.5
				political_power_daily < 0.5
			}
			modifier = {	# To make sure it doesn't work out as historically
				factor = 0
				is_historical_focus_on = yes
				NOT = { has_government = communism }
			}
			modifier = {
				factor = 10
				has_active_mission = SWE_the_progressive_tax_question
			}
		}
	}
	SWE_iden_om_folkhemmet = {
		allowed = {
			original_tag = SWE
		}
		visible = {
			OR = {
				date > 1928.1.1
				AND = {
					date > 1919.1.1
					OR = {
						has_completed_focus = SWE_secure_peace_within_the_left
						has_completed_focus = SWE_fuel_to_the_fire
					}
				}
			}
		}
		
		available = {
			OR = {
				has_government = communism
				has_completed_focus = SWE_welfare_reforms
				has_country_leader = {
					character = SWE_per_albin
					ruling_only = yes
				}
			}
			SWE_has_majority_for_reform_socialist = yes
		}
		fire_only_once = yes
		
		days_remove = 365
		cost = 50
		
		complete_effect = {
			SWE_increase_riksdag_rigidity = yes
		}
		remove_effect = {
			add_to_variable = {
				tooltip = swe_agr_dem_vote_f_tt
				var = SWE_agr_dem_votes_f
				value = -0.02
			}
			add_to_variable = {
				tooltip = swe_soc_dem_vote_f_tt
				var = SWE_soc_dem_votes_f
				value = 0.02
			}
			add_to_variable = {
				tooltip = swe_agr_dem_vote_a_tt
				var = SWE_agr_dem_votes_a
				value = -0.02
			}
			add_to_variable = {
				tooltip = swe_lib_dem_vote_a_tt
				var = SWE_lib_dem_votes_a
				value = -0.02
			}
			add_to_variable = {
				tooltip = swe_soc_dem_vote_a_tt
				var = SWE_soc_dem_votes_a
				value = 0.04
			}
			custom_effect_tooltip = skip_line
			custom_effect_tooltip = adjust_swe_economic_tt
			add_to_variable = {
				tooltip = consumer_goods_factor_tt
				var = SWE_consumer_goods
				value = 0.10
			}
			custom_effect_tooltip = adjust_swe_political_tt
			add_to_variable = {
				tooltip = political_power_factor_tt
				var = SWE_pol_political_power_factor
				value = 0.10
			}
			add_to_variable = {
				tooltip = stability_factor_tt
				var = SWE_pol_stability_factor
				value = 0.10
			}
			add_to_variable = {
				tooltip = research_speed_factor_tt
				var = SWE_pol_research_speed_factor
				value = 0.05
			}
			add_to_variable = {
				tooltip = communism_drift_tt
				var = SWE_pol_communism_drift
				value = 0.01
			}
			custom_effect_tooltip = adjust_emigration_problem_tt
			add_to_variable = {
				tooltip = monthly_population_tt
				var = SWE_emigration_monthly_population
				value = 0.05
			}
		}
		ai_will_do = {
			base = 1
			modifier = {
				factor = 0
				date < 1921.1.1
			}
		}
	}
	SWE_kommunal_skattereform = {
		# https://snd.se/sv/vivill/party/fp/v/1917
		# A desire to move more of income of state to schools, infrastructure and poor laws
		allowed = {
			original_tag = SWE
		}
		visible = {
			OR = {
				has_completed_focus = SWE_fix_what_is_backwards
				has_completed_focus = SWE_promote_reasonable_reforms
				has_completed_focus = SWE_welfare_reforms
				# It was implemented post war
				AND = {
					has_global_flag = great_war_ends
					date > 1917.1.1
					has_government = democratic
				}
			}
		}
		available = {
			OR = {
				has_country_leader_ideology = social_liberalism
				SWE_social_democracy_trigger = yes
				AND = {
					has_government = communism
					custom_trigger_tooltip = {
						tooltip = leftwing_seats_make_up_twentyfive_per
						OR = {
							check_variable = {
								var = liberal_f_riksdag
								value = 38
							}
							check_variable = {
								var = liberal_a_riksdag
								value = 58
							}
						}
					}
				}
			}
			SWE_has_majority_for_reform_social_liberal = yes
			NOT = { has_country_flag = riksdag_decisions_are_blocked }
		}
		fire_only_once = yes
		
		days_remove = 365
		cost = 50
		
		complete_effect = {
			SWE_increase_riksdag_rigidity = yes
			SWE_remove_concession_boost = yes
		}
		remove_effect = {
			custom_effect_tooltip = adjust_swe_political_tt
			add_to_variable = {
				tooltip = stability_factor_tt
				var = SWE_pol_stability_factor
				value = 0.05
			}
			add_to_variable = {
				tooltip = research_speed_factor_tt
				var = SWE_pol_research_speed_factor
				value = 0.01
			}
			custom_effect_tooltip = adjust_swe_economic_tt
			add_to_variable = {
				tooltip = consumer_goods_factor_tt
				var = SWE_consumer_goods
				value = -0.01
			}
			custom_effect_tooltip = adjust_emigration_problem_tt
			add_to_variable = {
				tooltip = monthly_population_tt
				var = SWE_emigration_monthly_population
				value = 0.03
			}
		}
		
		ai_will_do = {
			base = 100
		}
	}
	SWE_anti_trust_and_monopoly_reforms = {
		# https://snd.se/sv/vivill/party/fp/v/1920
		# Anti-monopoly stuff (monopolies and trusts being looked into, but otherwise no restrictions to be implemented)
		allowed = {
			tag = SWE
		}
		visible = {
			OR = {
				has_completed_focus = SWE_svensk_liberalism
				has_completed_focus = SWE_a_mixed_economy_solution
			}
		}
		available = {
			OR = {
				AND = {
					OR = {
						has_country_leader_ideology = social_liberalism
						SWE_social_democracy_trigger = yes
					}
				}
				AND = {
					OR = {
						SWE_social_democracy_trigger = yes
						has_government = communism
					}
				}
			}
			if = {
				limit = {
					OR = {
						has_country_leader_ideology = social_liberalism
						has_country_leader_ideology = social_democracy
					}
				}
				SWE_has_majority_for_reform_liberalism = yes
			}
			else_if = {
				limit = {
					OR = {
						has_country_leader_ideology = social_democracy
						has_government = communism
					}
				}
				SWE_has_majority_for_reform_socialist = yes
			}
			else = {
				SWE_has_majority_for_reform_socialist = yes
				hidden_trigger = { always = no }
			}
		}
		fire_only_once = yes
		
		days_remove = 365
		cost = 100
		
		complete_effect = {
			SWE_increase_riksdag_rigidity = yes
			SWE_remove_concession_boost = yes
		}
		remove_effect = {
			custom_effect_tooltip = adjust_swe_economic_tt
			add_to_variable = {
				tooltip = political_power_cost_tt
				var = SWE_eco_political_power_cost
				value = 0.10
			}
			add_to_variable = {
				tooltip = stability_factor_tt
				var = SWE_eco_stability
				value = 0.03
			}
			add_to_variable = {
				tooltip = consumer_goods_factor_tt
				var = SWE_consumer_goods
				value = -0.03
			}
		}
		
		ai_will_do = {
			base = 1
		}
	}
	SWE_reduce_state_waste = {
		# https://snd.se/sv/vivill/party/fp/v/1920 (and 1917)
		# Wishing to reduce the state's waste of money essentially
		allowed = {
			tag = SWE
		}
		visible = {
			has_completed_focus = SWE_svensk_liberalism
		}
		available = {
			OR = {
				has_country_leader_ideology = social_liberalism
				has_country_leader_ideology = classical_liberalism
			}
			SWE_has_majority_for_reform_liberalism = yes
		}
		fire_only_once = yes
		
		days_remove = 210
		cost = 50
		
		complete_effect = {
			SWE_increase_riksdag_rigidity = yes
			SWE_remove_concession_boost = yes
		}
		remove_effect = {
			custom_effect_tooltip = adjust_swe_economic_tt
			add_to_variable = {
				tooltip = political_power_cost_tt
				var = SWE_eco_political_power_cost
				value = -0.15
			}
			# Maybe represent it differently in future
			add_to_variable = {
				tooltip = production_speed_buildings_factor_tt
				var = SWE_build_speed
				value = -0.03
			}
		}
		
		ai_will_do = {
			base = 1
		}
	}
	SWE_reduction_of_unnessecary_tariffs = {
		allowed = {
			tag = SWE
		}
		visible = {
			date > 1917.1.1
			has_country_flag = {
				flag = swe_customs_flag
				days > 365
			}
			NOT = {
				check_variable = {
					var = SWE_min_export
					value = -0.04
				}
			}
		}
		
		available = {
			OR = {
				has_country_leader_ideology = social_liberalism
				SWE_social_democracy_trigger = yes
				has_country_leader_ideology = classical_liberalism	# No one to fit this rn technically afaik
				has_government = communism
			}
			OR = {
				has_completed_focus = SWE_svensk_liberalism
				has_completed_focus = SWE_a_mixed_economy_solution	# Maybe remove this one
				AND = {
					has_completed_focus = SWE_resource_market
					has_global_flag = great_war_ends
				}
			}
			SWE_has_majority_for_reform_social_liberal = yes
		}
		fire_only_once = yes
		
		days_remove = 50
		cost = 25
		
		complete_effect = {
			SWE_remove_concession_boost = yes
		}
		
		remove_effect = {
			custom_effect_tooltip = adjust_swe_economic_tt
			add_to_variable = {
				tooltip = political_power_gain_tt
				var = SWE_eco_political_power_gain
				value = -0.03
			}
			add_to_variable = {
				tooltip = min_export_tt
				var = SWE_min_export
				value = 0.05
			}
			add_to_variable = {
				tooltip = consumer_goods_factor_tt
				var = SWE_consumer_goods
				value = -0.02
			}
		}
		
		ai_will_do = {
			base = 10
		}
	}
	SWE_discussion_around_current_tariff = {
		# https://www.riksdagen.se/sv/dokument-och-lagar/dokument/motion/motioner-i-forsta-hammaren-nr-11_d92c11/
			# Conclussion: Lower it
		allowed = {
			tag = SWE
		}
		visible = {
			date > 1912.12.1
			NOT = { has_country_flag = swe_customs_flag }
		}
		
		available = {
			has_war = no
		}
		fire_only_once = yes
		
		days_remove = 25
		cost = 25
		
		complete_effect = {
			#country_event = { id = ww_sverige_riksdag.0 }
			set_country_flag = swe_customs_flag
		}
		remove_effect = {
			country_event = { id = ww_sverige_riksdag.1 }
			custom_effect_tooltip = SWE_discussion_around_current_tariff_tt
			#event_option_tooltip = ww_sverige_riksdag.1.a
		}
		
		ai_will_do = {
			base = 10
			modifier = {
				factor = 10
				date > 1913.1.1
			}
		}
	}
}
# Riksdagen - IDK
SWE_riksdagens_decisions = {
	SWE_invest_into_universities = {	# Ernst Trygger one
		# He focused a lot on higher education
			# Will look into specifics
		allowed = {
			original_tag = SWE
		}
		visible = {
			date > 1915.1.1
			has_country_leader = {
				character = SWE_ernst_trygger
				ruling_only = yes
			}
		}
		available = {
			has_war = no
			has_country_leader = {
				character = SWE_ernst_trygger
				ruling_only = yes
			}
		}
		fire_only_once = yes
		
		days_remove = 210
		cost = 100
		
		remove_effect = {
			custom_effect_tooltip = adjust_swe_political_tt
			add_to_variable = {	# Change to consumer goods?
				tooltip = drift_defence_factor_tt
				var = SWE_pol_drift_defence_factor
				value = -0.05
			}
			add_to_variable = {
				tooltip = research_speed_factor_tt
				var = SWE_pol_research_speed_factor
				value = 0.02
			}
		}
		
		ai_will_do = {
			base = 0
			modifier = {
				add = 100
				date > 1916.1.1
			}
		}
	}
	SWE_introduce_arbetsloshetskommissionen = {
		# Statens arbetslöshetskommission
		allowed = {
			original_tag = SWE
		}
		available = {
			NOT = { has_country_flag = riksdag_decisions_are_blocked }
		}
		visible = {
			OR = {
				has_global_flag = great_war
				date > 1914.6.1
			}
		}
		
		fire_only_once = yes
		
		days_remove = 90
		cost = 35
		
		remove_effect = {
			set_country_flag = swe_arbetsloshetskommissionen
			custom_effect_tooltip = adjust_swe_economic_tt
			add_to_variable = {
				tooltip = stability_factor_tt
				var = SWE_eco_stability
				value = 0.05
			}
		}
		
		ai_will_do = {
			base = 1
			modifier = {
				factor = 10
				has_stability > 0.5
			}
		}
	}
	SWE_introduce_handelsdepartementet = {
		# Handelsdepartementet
		allowed = {
			original_tag = SWE
		}
		available = {
			NOT = { has_country_flag = riksdag_decisions_are_blocked }
		}
		visible = {
			date > 1919.1.1
		}
		fire_only_once = yes
		
		days_remove = 90
		cost = 35
		
		remove_effect = {
			custom_effect_tooltip = adjust_swe_political_tt
			add_to_variable = {
				tooltip = trade_laws_cost_factor_tt
				var = SWE_pol_trade_laws_cost_factor
				value = -0.10
			}
		}
		
		ai_will_do = {
			base = 1
			modifier = {
				factor = 10
				has_stability > 0.5
			}
		}
	}
	# TBA: Stavningsreform 
	# den stora fattigvårdsreformen 1918
	SWE_fattigvardslag = {
		# Fattigauktion
		# Let's ban like. bidding to take care of the poorest and most vulnerable for lowest cost in exchange for some benefits cuz law said so...
		allowed = {
			original_tag = SWE
		}
		visible = {
			date > 1917.1.1
			#has_dynamic_modifier = { modifier = SWE_emigration_problem }
		}
		available = {
			has_war = no
			SWE_has_majority_for_reform_social_liberal = yes
			NOT = { has_country_flag = riksdag_decisions_are_blocked }
		}
		
		cost = 100
		
		days_remove = 45
		fire_only_once = yes
		
		modifier = {
			consumer_goods_factor = 0.15
		}
		
		complete_effect = {
			add_stability = 0.025
			add_war_support = 0.025
		}
		
		remove_effect = {
			custom_effect_tooltip = adjust_swe_political_tt
			add_to_variable = {
				tooltip = stability_factor_tt
				var = SWE_pol_stability_factor
				value = 0.03
			}
			custom_effect_tooltip = adjust_emigration_problem_tt
			add_to_variable = {
				tooltip = monthly_population_tt
				var = SWE_emigration_monthly_population
				value = 0.03
			}
		}
		
		ai_will_do = {
			base = 0
			modifier = {
				add = 100
				date > 1918.5.1
			}
		}
	}
	SWE_campaign_to_stay_at_home = {	# Generic, to be replaced
		# Stop leaving Sweden!
		allowed = {
			original_tag = SWE
		}
		visible = {
			has_completed_focus = SWE_keeping_our_head_down
			has_dynamic_modifier = { modifier = SWE_emigration_problem }
		}
		available = {
			has_war = no
			threat > 0.5
			has_stability > 0.7
			has_completed_focus = SWE_investera_i_framtiden
			NOT = { has_idea = SWE_hungerskjold }
			NOT = { has_country_flag = riksdag_decisions_are_blocked }
		}
		
		cost = 25
		days_remove = 240
		fire_only_once = yes
		
		remove_effect = {
			custom_effect_tooltip = adjust_emigration_problem_tt
			add_to_variable = {
				tooltip = monthly_population_tt
				var = SWE_emigration_monthly_population
				value = 0.05
			}
		}
		
		ai_will_do = {
			base = 0.1
		}
	}
	SWE_telgraf_och_telefon_utveckling = {
		icon = GFX_decision_hol_exchange_intelligence_data
		allowed = {
			tag = SWE
		}
		
		fire_only_once = yes
		available = {
			has_capitulated = no
		}
		visible = {
			date > 1912.1.1
		}
		
		cost = 35
		
		complete_effect = {
			add_tech_bonus = {
				name = mechanical_innovation
				category = electronics
				uses = 1
				bonus = 0.5
			}
		}
		ai_will_do = {
			base = 0
			modifier = {
				add = 5
				date > 1913.1.1
			}
			modifier = {
				add = 5
				date > 1913.2.1
			}
			modifier = {
				factor = 5
				date > 1913.3.1
			}
		}
	}
	SWE_sweeping_political_restructuring = {	# Generic, to be replaced
		# We'll not allow further dangers to Democracy in Sweden and will implement a number of reforms to stop it from being abused
		# Short-term stability loss in return for a bit of pp and stability return
		allowed = {
			original_tag = SWE
		}
		visible = {
			OR = {
				has_completed_focus = SWE_fix_what_is_backwards
				has_completed_focus = SWE_social_democratic_government
			}
		}
		
		available = {
			has_civil_war = no
			OR = {
				has_country_leader_ideology = social_liberalism
				SWE_social_democracy_trigger = yes
			}
			has_stability > 0.6
			fascism < 0.10
			neutrality < 0.10
			NOT = { has_country_flag = riksdag_decisions_are_blocked }
		}
		fire_only_once = yes
		
		days_remove = 210
		
		modifier = {
			stability_weekly = -0.005
		}
		
		cost = 150
		
		complete_effect = {
			add_stability = -0.05
			add_war_support = -0.05
		}
		
		remove_effect = {
			add_timed_idea = {
				idea = SWE_democratic_stronghold
				days = 730
			}
			custom_effect_tooltip = skip_line
			add_to_variable = {
				tooltip = swe_con_dem_vote_f_tt
				var = SWE_con_dem_votes_f
				value = -0.05
			}
			add_to_variable = {
				tooltip = swe_agr_dem_vote_f_tt
				var = SWE_agr_dem_votes_f
				value = 0.03
			}
			add_to_variable = {
				tooltip = swe_soc_dem_vote_f_tt
				var = SWE_soc_dem_votes_f
				value = 0.02
			}
			custom_effect_tooltip = skip_line
			add_to_variable = {
				tooltip = swe_con_dem_vote_a_tt
				var = SWE_con_dem_votes_a
				value = -0.04
			}
			add_to_variable = {
				tooltip = swe_lib_dem_vote_a_tt
				var = SWE_lib_dem_votes_a
				value = 0.02
			}
			add_to_variable = {
				tooltip = swe_soc_dem_vote_a_tt
				var = SWE_soc_dem_votes_a
				value = 0.02
			}
		}
		ai_will_do = {
			base = 0.1
		}
	}
	SWE_porjus_kraftverk = {
		# Electric production in north pls
		icon = GFX_decision_generic_industry
		allowed = {
			original_tag = SWE
		}
		available = {
			has_stability > 0.5
			controls_state = 666
		}
		
		fire_only_once = yes
		visible = {
			date > 1912.1.1
			666 = { NOT = { has_state_flag = porjus_kraftverk } }
		}
		
		cost = 50
		days_remove = 730
		
		modifier = { consumer_goods_factor = 0.03 }
		
		remove_effect = {
			666 = {
				set_state_flag = porjus_kraftverk
				add_extra_state_shared_building_slots = 1
				if = {
					limit = {
						free_building_slots = {
							building = industrial_complex
							size > 0
							include_locked = yes
						}
					}
					add_building_construction = {
						type = industrial_complex
						level = 1
						instant_build = yes
					}
				}
			}
		}
		
		ai_will_do = {
			base = 0
			modifier = {
				add = 100
				date > 1912.12.1
			}
		}
	}
	SWE_purchase_sodertalje_kanal_och_slussverksaktiebolag = {
		# Södertälje kanal- och slussverksaktiebolags 
		icon = GFX_decision_generic_industry
		allowed = {
			tag = SWE
		}
		available = {
			controls_state = 141
		}
		
		fire_only_once = yes
		visible = {
			has_completed_focus = SWE_funds_to_buy_industries
		}
		
		cost = 25
		days_remove = 240
		
		modifier = { consumer_goods_factor = 0.05 }
		
		remove_effect = {
			141 = {
				if = {
					limit = {
						free_building_slots = {
							building = infrastructure
							size > 0
						}
					}
					add_building_construction = {
						type = infrastructure
						level = 1
						instant_build = yes
					}
				}
				add_extra_state_shared_building_slots = 1
			}
		}
		
		ai_will_do = {
			base = 0
			modifier = {
				add = 100
				date > 1912.1.1
			}
		}
	}
	SWE_purchase_important_industries = {
		icon = GFX_decision_generic_industry
		allowed = {
			tag = SWE
		}
		available = {
			NOT = { has_country_flag = riksdag_decisions_are_blocked }
		}
		
		fire_only_once = yes
		visible = {
			has_completed_focus = SWE_funds_to_buy_industries
			any_owned_state = {
				free_building_slots = {
					building = industrial_complex
					size > 0
					include_locked = yes
				}
			}
		}
		
		cost = 100
		days_remove = 140
		
		modifier = { consumer_goods_factor = 0.15 }
		
		remove_effect = {
			random_owned_controlled_state = {
				limit = {
					free_building_slots = {
						building = industrial_complex
						size > 0
						include_locked = yes
					}
					OR = {
						is_in_home_area = yes
						NOT = {
							owner = {
								any_owned_state = {
									free_building_slots = {
										building = industrial_complex
										size > 0
										include_locked = yes
									}
									is_in_home_area = yes
								}
							}
						}
					}
				}
				add_extra_state_shared_building_slots = 1
				add_building_construction = {
					type = industrial_complex
					level = 1
					instant_build = yes
				}
			}
		}
		
		ai_will_do = {
			base = 1
			modifier = {
				add = 1
				date > 1912.3.1
			}
			modifier = {
				add = 1
				date > 1912.6.1
			}
			modifier = {
				add = 1
				date > 1912.9.1
			}
			modifier = {
				add = 1
				date > 1912.12.1
			}
			modifier = {
				factor = 10
				date > 1913.1.1
			}
		}
	}
	# Agrarian Reforms (Look at Agrarian parties for inspiration)
	SWE_electrify_our_railways = {
		allowed = {
			tag = SWE
		}
		visible = {
			has_completed_focus = SWE_keeping_our_head_down
		}
		available = {
			has_completed_focus = SWE_investera_i_framtiden
			141 = { is_fully_controlled_by = ROOT }
			666 = { is_fully_controlled_by = ROOT }
		}
		
		cost = 25
		days_remove = 240
		fire_only_once = yes
		
		remove_effect = {
			if = {
				limit = { NOT = { has_tech = electric_railway } }
				add_tech_bonus = {
					name = electric_railway
					bonus = 0.5
					uses = 1
					technology = electric_railway
				}
			}
			else_if = {
				limit = { NOT = { has_tech = diesel_electric_control_system } }
				add_tech_bonus = {
					name = diesel_electric_control_system
					bonus = 0.5
					uses = 1
					technology = diesel_electric_control_system
				}
			}
			build_railway = {	# For now, arbitrary
				level = 1
				build_only_on_allied = yes
				start_province = 9209
				target_province = 3063
			}
			build_railway = {
				level = 1
				build_only_on_allied = yes
				path = { 9193 3106 9185 9209 6188 3063 }
				start_province = 9193
				target_province = 3063
			}
			build_railway = {
				level = 1
				build_only_on_allied = yes
				path = { 101 3095 11155 9226 9186 3104 9108 }
				start_province = 101
				target_province = 9108
			}
			# Proper Bonus here
			add_tech_bonus = {
				name = train_tech
				category = train_tech
				uses = 1
				bonus = 0.25
			}
		}
		
		modifier = {
			consumer_goods_factor = 0.05
		}
		
		ai_will_do = {
			base = 100
		}
	}
	# TBA:
		# https://lagen.nu/prop/1914:b57 (war court-related)
		# https://lagen.nu/prop/1914:b232 (tariff related)
		# Reform tax system (1919/Post-WW1)
		# Statens arbetslöshetskommission - very important post-war
		# Statens industrikommission (during ww1)
		# Lag om barn utom äktenskap
		# Ensittarlagen
		# Sprängämnesinspektionen
}
# Decisions Around Finding Allies (Communist n Proto-Fascist)
SWE_riksdagens_decisions = {
	SWE_find_anti_monarchist_allies = {
		allowed = {
			tag = SWE
		}
		visible = {
			has_completed_focus = SWE_fuel_to_the_fire
		}
		available = {
			OR = {
				has_government = communism
				SWE_social_democracy_trigger = yes
			}
			custom_trigger_tooltip = {
				tooltip = SWE_anti_communist_allies_enough_supporters
				set_temp_variable = {
					var = support_for_reform_a
					value = V_a
				}
				add_to_temp_variable = {
					var = support_for_reform_a
					value = SKP_a
				}
				set_temp_variable = {
					var = support_for_reform_f
					value = V_f
				}
				OR = {
					communism > 0.399
					AND = {
						check_temp_variable = { support_for_reform_a = 50 }
						check_temp_variable = { support_for_reform_f = 3 }
					}
				}
			}
		}
		
		cost = 50
		days_remove = 50
		fire_only_once = yes
		priority = 3
		
		modifier = {
			democratic_drift = -0.03
			communism_drift = 0.03
		}
		
		remove_effect = {
			add_to_variable = {
				tooltip = swe_soc_soc_vote_f_tt
				var = SWE_soc_soc_votes_f
				value = -0.03
			}
			add_to_variable = {
				tooltip = swe_com_soc_vote_f_tt
				var = SWE_com_soc_votes_f
				value = 0.03
			}
			custom_effect_tooltip = skip_line
			add_to_variable = {
				tooltip = swe_soc_soc_vote_a_tt
				var = SWE_soc_soc_votes_a
				value = -0.04
			}
			add_to_variable = {
				tooltip = swe_com_soc_vote_a_tt
				var = SWE_com_soc_votes_a
				value = 0.04
			}
		}
		
		ai_will_do = {
			base = 10
		}
	}
	SWE_pressure_a_popular_front = {
		allowed = {
			tag = SWE
		}
		visible = {
			has_completed_focus = SWE_fuel_to_the_fire
		}
		available = {
			has_government = democratic
			OR = {
				SWE_has_majority_for_reform_socialist = yes
				AND = {
					democratic < 0.2
					communism > 0.4
				}
			}
		}
		
		cost = 75
		days_remove = 50
		fire_only_once = yes
		priority = 3
		
		remove_effect = {
			add_stability = -0.05
			add_war_support = -0.05
			set_temp_variable = {
				var = neutrality_support_var
				value = party_popularity_100@neutrality
			}
			set_temp_variable = {
				var = democratic_support_var
				value = party_popularity_100@democratic
			}
			multiply_temp_variable = {
				var = democratic_support_var
				value = 0.2
			}
			set_temp_variable = {
				var = democratic_support_var_two
				value = party_popularity_100@democratic
			}
			subtract_from_temp_variable = {
				var = democratic_support_var_two
				value = democratic_support_var
			}
			set_temp_variable = {
				var = communism_support_var
				value = party_popularity_100@communism
			}
			set_temp_variable = {
				var = fascism_support_var
				value = party_popularity_100@fascism
			}
			add_to_temp_variable = {
				communism_support_var = democratic_support_var
			}
			custom_effect_tooltip = a_fifth_of_dem_will_join_communist
			hidden_effect = {
				set_popularities = {
					democratic = democratic_support_var_two
					neutrality = neutrality_support_var
					fascism = fascism_support_var
					communism = communism_support_var
				}
			}
		}
		
		ai_will_do = {
			base = 10
		}
	}
	SWE_hold_limited_elections = {
		allowed = {
			tag = SWE
		}
		visible = {
			has_elections = no
			OR = {
				has_government = fascism
				has_government = neutrality
			}
			has_completed_focus = SWE_military_control
			NOT = {
				has_completed_focus = SWE_restoration_of_democracy
			}
		}
		available = {
			democratic < 0.07
			communism < 0.07
			has_stability > 0.6
			NOT = { has_idea = SWE_the_limited_government }
		}
		
		days_remove = 30
		days_re_enable = 365
		cost = 50
		priority = 5
		
		remove_effect = {
			custom_effect_tooltip = adjust_swe_political_tt
			add_to_variable = {
				tooltip = political_power_cost_tt
				var = SWE_pol_political_power_cost
				value = -0.05
			}
			country_event = { id = ww_sverige_demo_crisis.14 }
		}
		
		ai_will_do = {
			base = 2
		}
	}
	SWE_realign_conservative_to_na_in_forsta_kammaren = {
		allowed = {
			tag = SWE
		}
		visible = {
			has_idea = SWE_the_limited_government
			NOT = {
				check_variable = {
					var = NAP_f
					value = 40
				}
			}
		}
		available = {
			fascism > 0.25
			custom_trigger_tooltip = {
				tooltip = can_take_seats_from_h_f_tt
				check_variable = {
					var = H_f
					value = 10
				}
			}
		}
		cancel_trigger = {
			NOT = { has_idea = SWE_the_limited_government }
		}
		
		days_remove = 14
		priority = 5
		
		complete_effect = {
			add_popularity = {
				ideology = fascism
				popularity = -0.03
			}
		}
		
		remove_effect = {
			add_to_variable = {
				tooltip = H_f_mandates_tt
				var = H_f
				value = -10
			}
			add_to_variable = {
				tooltip = NP_f_mandates_tt
				var = NAP_f
				value = 10
			}
			SWE_init_first_chamber = yes
		}
		
		ai_will_do = {
			base = 1
			modifier = {
				factor = 0
				fascism < 0.28
			}
			modifier = {
				factor = 0
				check_variable = {
					var = NAP_f
					value = 40
				}
			}
		}
	}
	SWE_realign_conservative_to_na_in_andra_kammaren = {
		allowed = {
			tag = SWE
		}
		visible = {
			has_idea = SWE_the_limited_government
			NOT = {
				check_variable = {
					var = NP_a
					value = 58
				}
			}
		}
		available = {
			fascism > 0.25
			custom_trigger_tooltip = {
				tooltip = can_take_seats_from_av_a_tt
				check_variable = {
					var = AV_a
					value = 8
				}
			}
		}
		cancel_trigger = {
			NOT = { has_idea = SWE_the_limited_government }
		}
		
		days_remove = 14
		priority = 10
		
		complete_effect = {
			add_popularity = {
				ideology = fascism
				popularity = -0.03
			}
		}
		
		remove_effect = {
			add_to_variable = {
				tooltip = AV_a_mandates_tt
				var = AV_a
				value = -8
			}
			add_to_variable = {
				tooltip = NP_a_mandates_tt
				var = NP_a
				value = 8
			}
			SWE_init_second_chamber = yes
		}
		
		ai_will_do = {
			base = 1
			modifier = {
				factor = 0
				fascism < 0.28
			}
			modifier = {
				factor = 0
				check_variable = {
					var = NP_a
					value = 58
				}
			}
		}
	}
	SWE_unite_the_right_in_crisis = {
		allowed = {
			original_tag = SWE
		}
		visible = {
			has_idea = SWE_unpopular_riksdag
		}
		available = {
			OR = {
				threat > 0.4
				has_defensive_war = yes
			}
			has_completed_focus = SWE_allies_in_unlikely_places
		}
		cancel_trigger = {
			hidden_trigger = {
				NOT = { has_idea = SWE_unpopular_riksdag }
			}
		}
		
		cost = 50
		
		days_remove = 70
		priority = 5
		
		remove_effect = {
			remove_ideas = SWE_unpopular_riksdag
		}
		
		ai_will_do = {
			base = 1
			modifier = {
				factor = 10
				date > 1915.1.1
			}
		}
	}
}
# Military - Riksdag
SWE_riksdagens_decisions = {
	SWE_realign_the_military_budget = {
		# This is here to return budget to 'normal levels' more quickly
		allowed = {
			original_tag = SWE
		}
		visible = {
			has_idea = SWE_forsvarsvasendet
			OR = {
				has_completed_focus = SWE_courtyard_speech
				has_completed_focus = SWE_third_liberal_regering
			}
		}
		available = {
			OR = {
				has_war_support > 0.2
				threat > 0.15
			}
			# Nothing for now, fix later
			NOT = {
				has_country_leader = {
					character = SWE_karl_staaff
					ruling_only = yes
				}
			}
		}
		fire_only_once = yes
		
		cost = 20
		priority = 2
		
		complete_effect = {
			remove_ideas = SWE_forsvarsvasendet
		}
		
		ai_will_do = {
			base = 1
			modifier = {
				factor = 2
				is_great_war_ongoing = yes
			}
			modifier = {
				factor = 2
				NOT = {
					has_country_leader_ideology = social_liberalism
					has_country_leader_ideology = social_democracy
				}
			}
			modifier = {
				factor = 2
				date > 1920.1.1
			}
		}
	}
	SWE_forsoks_mobiiseringen_1912 = {
		# Try to see how well Sweden could mobilize in 1912, decided in 1911
		allowed = {
			original_tag = SWE
		}
		visible = {
			has_completed_focus = SWE_second_karl_staaff_regeringen
			date < 1912.1.2
		}
		available = {
			has_war = no
			date < 1912.1.1
		}
		
		custom_cost_trigger = {
			command_power > 24
			has_political_power > 24
		}
		custom_cost_text = decision_cost_CP_25_pp_25
		
		days_remove = 21
		fire_only_once = yes
		priority = 2
		
		complete_effect = {
			hidden_effect = {
				add_command_power = -25
				add_political_power = -25
			}
			set_country_flag = {
				flag = SWE_forsoksmobilisering
				days = 400
				value = 1
			}
		}
		
		remove_effect = {
			custom_effect_tooltip = if_the_decision_been_passed_the_following_will_be_gained_in_training_next_year
			effect_tooltip = {
				add_war_support = 0.03
				custom_effect_tooltip = adjust_swe_military_tt
				add_to_variable = {
					tooltip = mobilization_speed_tt
					var = SWE_mil_mobilization_speed
					value = 0.05
				}
			}
		}
		
		ai_will_do = {
			base = 1
			modifier = {
				factor = 10
				date > 1911.6.1
			}
			modifier = {
				factor = 10
				date > 1911.9.1
			}
		}
	}
	SWE_motion_for_military_expansion = {
		allowed = {
			original_tag = SWE
		}
		visible = {
			OR = {
				has_completed_focus = SWE_keeping_our_head_down
				has_completed_focus = SWE_foreign_policy_of_the_king
				has_completed_focus = SWE_creation_of_nationalistiskt_arbetarparti
			}
		}
		available = {
			has_completed_focus = SWE_support_the_military
			SWE_has_majority_for_reform_war = yes
			NOT = { has_idea = idea_army_restrictions }
		}
		
		custom_cost_trigger = {
			command_power > 24
			has_political_power > 49
		}
		custom_cost_text = decision_cost_CP_25_pp_50
		
		days_remove = 175
		fire_only_once = yes
		
		complete_effect = {
			hidden_effect = {
				add_command_power = -25
				add_political_power = -50
			}
			activate_mission = SWE_goal_of_army_bill
		}
		
		modifier = {
			war_support_weekly = 0.002
		}
		
		remove_effect = {
			army_experience = 15
			custom_effect_tooltip = adjust_swe_military_tt
			add_to_variable = {
				tooltip = training_time_factor_tt
				var = SWE_mil_training_time_factor
				value = -0.05
			}
			add_to_variable = {
				tooltip = mobilization_speed_tt
				var = SWE_mil_mobilization_speed
				value = 0.05
			}
			custom_effect_tooltip = adjust_swe_economic_tt
			add_to_variable = {
				tooltip = industrial_capacity_factory_tt
				var = SWE_mil_industrial_capacity_factory
				value = 0.01
			}
		}
		
		ai_will_do = {
			base = 1
			modifier = {	# Just to make sure pp is spent properly
				factor = 0
				OR = {
					has_political_power < 151
					AND = {
						has_war = yes
						OR = {
							has_idea = disarmed_nation
							has_idea = volunteer_only
						}
					}
				}
			}
		}
	}
	SWE_early_conscription_extension = {
		allowed = {
			original_tag = SWE
		}
		visible = {
			has_completed_focus = SWE_short_term_preparations
		}
		available = {
			OR = {
				NOT = { has_idea = SWE_anti_war_promises }
				AND = {
					has_stability > 0.7
					has_war_support > 0.4
				}
			}
			SWE_has_majority_for_reform_war_light = yes
			OR = {
				threat > 0.15
				has_war = yes
			}
			NOT = {
				has_idea = idea_armistice
				has_idea = idea_army_restrictions
			}
		}
		
		custom_cost_trigger = {
			command_power > 24
			has_political_power > 74
		}
		custom_cost_text = decision_cost_CP_25_pp_75
		
		days_remove = 50
		fire_only_once = yes
		
		complete_effect = {
			SWE_increase_riksdag_rigidity = yes
			add_stability = -0.05
			hidden_effect = {
				add_command_power = -25
				add_political_power = -75
			}
		}
		
		remove_effect = {
			add_manpower = 2000
			if = {
				limit = {
					OR = {
						has_idea = disarmed_nation
						has_idea = volunteer_only
					}
				}
				add_ideas = limited_conscription
			}
		}
		
		ai_will_do = {
			base = 2
			modifier = {
				factor = 5
				has_war = yes
			}
			modifier = {
				factor = 0
				has_political_power < 150
			}
		}
	}
	SWE_funds_to_army_jobs = {
		# Let's bring in those funds!
		icon = GFX_decision_generic_industry
		allowed = {
			original_tag = SWE
		}
		available = {
			NOT = { has_idea = SWE_anti_war_promises }
			any_owned_state = {
				is_controlled_by = ROOT
				arms_factory > 0
			}
			# Add check
			OR = {
				has_war_support > 0.15
				threat > 0.1
			}
			SWE_has_majority_for_reform_war = yes
		}
		
		fire_only_once = yes
		
		visible = {
			has_completed_focus = SWE_create_army_jobs
			any_owned_state = {
				is_controlled_by = ROOT
				free_building_slots = {
					building = arms_factory
					size > 0
					include_locked = yes
				}
			}
		}
		
		cost = 50
		days_remove = 365
		
		modifier = {
			consumer_goods_factor = 0.10
		}
		
		complete_effect = {
			if = {
				limit = { has_war = no }
				SWE_increase_riksdag_rigidity = yes
			}
		}
		remove_effect = {
			random_owned_controlled_state = {
				limit = {
					is_controlled_by = ROOT
					free_building_slots = {
						building = arms_factory
						size > 0
						include_locked = yes
					}
				}
				add_extra_state_shared_building_slots = 1
				add_building_construction = {
					type = arms_factory
					level = 1
					instant_build = yes
				}
			}
		}
		ai_will_do = {
			base = 0
			modifier = {
				add = 1
				OR = {
					has_country_leader_ideology = conservatism
					has_country_leader_ideology = liberal_conservatism
				}
			}
			modifier = {
				add = 1
				date > 1913.1.1
			}
			modifier = {
				add = 1
				has_war = yes
			}
			modifier = {
				add = 1
				is_great_war_ongoing = yes
			}
			modifier = {
				factor = 0.5
				has_idea = civilian_economy
			}
		}
	}
	SWE_extensively_expand_army_budget = {
		allowed = {
			original_tag = SWE
		}
		visible = {
			has_completed_focus = SWE_favor_gustaf_hammarskjold
		}
		available = {
			OR = {
				threat > 0.4
				has_war = yes
			}
			has_war_support > 0.7
			SWE_has_majority_for_reform_war = yes
			NOT = {
				has_idea = idea_armistice
				has_idea = idea_army_restrictions
			}
		}
		
		cost = 100
		
		days_remove = 140
		fire_only_once = yes
		
		modifier = {
			stability_factor = -0.05
		}
		
		remove_effect = {
			custom_effect_tooltip = adjust_swe_political_tt
			add_to_variable = {
				tooltip = war_support_factor_tt
				var = SWE_pol_war_support_factor
				value = -0.05
			}
			custom_effect_tooltip = adjust_swe_economic_tt
			add_to_variable = {
				tooltip = consumer_goods_factor_tt
				var = SWE_consumer_goods
				value = 0.10
			}
			add_to_variable = {
				tooltip = industrial_capacity_factory_tt
				var = SWE_mil_industrial_capacity_factory
				value = 0.05
			}
			add_to_variable = {
				tooltip = industrial_capacity_dockyard_tt
				var = SWE_mil_industrial_capacity_dockyard
				value = 0.05
			}
		}
		
		ai_will_do = {
			base = 1
			modifier = {
				factor = 10
				has_war = yes
			}
			modifier = {
				factor = 0
				has_political_power < 150
			}
		}
	}
}
# Riksdagen - Conservative
SWE_riksdagens_decisions = {
	SWE_put_paragraf_23_to_law = {
		# https://sv.wikipedia.org/wiki/Paragraf_32
		# Was called Paragaf 23 at the time
			# Essentially a worker cannot just refuse to work without due cause
		allowed = {
			tag = SWE
		}
		visible = {
			NOT = { has_government = communism }
			NOT = { has_completed_focus = SWE_third_liberal_regering }
			NOT = { has_country_flag = swe_handled_class_conflict }
		}
		available = {
			communism < 0.1
			OR = {
				custom_trigger_tooltip = {
					tooltip = sixty_percent_rightwing_support
					set_temp_variable = { rightwing_temp = party_popularity@fascism }
					add_to_temp_variable = { rightwing_temp = party_popularity@neutrality }
					check_temp_variable = {
						var = rightwing_temp
						value = 0.6
					}
				}
				has_completed_focus = SWE_ban_dangerous_socialist_organisations
			}
			custom_trigger_tooltip = {
				tooltip = majority_con_riksdag_f_tt
				set_temp_variable = { riksdag_f_temp = conservative_f_riksdag }
				check_temp_variable = {
					var = riksdag_f_temp
					value = 76
				}
			}
			custom_trigger_tooltip = {
				tooltip = majority_con_riksdag_a_tt
				set_temp_variable = { riksdag_a_temp = conservative_a_riksdag }
				check_temp_variable = {
					var = riksdag_a_temp
					value = 116
				}
			}
			has_stability > 0.8
			has_completed_focus = SWE_courtyard_speech
			NOT = { has_country_flag = riksdag_decisions_are_blocked }
		}
		fire_only_once = yes
		
		days_remove = 600
		cost = 100
		
		modifier = {
			stability_factor = -0.1
		}
		
		complete_effect = {
			SWE_increase_riksdag_rigidity = yes
			set_country_flag = swe_handled_class_conflict
			# Event about temporary strikes in response
		}
		
		remove_effect = {
			if = {
				limit = { NOT = { has_idea = SWE_class_conflict } }
				add_ideas = SWE_paragraf_twenty_three
			}
			else = {
				swap_ideas = {
					add_idea = SWE_paragraf_twenty_three
					remove_idea = SWE_class_conflict
				}
			}
			custom_effect_tooltip = adjust_swe_political_tt
			add_to_variable = {
				tooltip = stability_factor_tt
				var = SWE_pol_stability_factor
				value = -0.05
			}
			add_to_variable = {
				tooltip = communism_drift_tt
				var = SWE_pol_communism_drift
				value = -0.01
			}
			custom_effect_tooltip = adjust_emigration_problem_tt
			add_to_variable = {
				tooltip = monthly_population_tt
				var = SWE_emigration_monthly_population
				value = -0.02
			}
			# Follow-up with some temporary penalties from opposition
		}
		ai_will_do = {
			base = 0
			modifier = {
				add = 1
				has_completed_focus = SWE_ban_dangerous_socialist_organisations
			}
			modifier = {
				add = 1
				has_completed_focus = SWE_military_control
			}
		}
	}
	SWE_further_concessions_to_large_businesses = {
		allowed = {
			original_tag = SWE
		}
		visible = {
			has_idea = SWE_protecting_interests_of_businessowners
		}
		available = {
			SWE_has_majority_for_reform_conservative = yes
			OR = {
				has_completed_focus = SWE_kungliga_regeringen
				has_completed_focus = SWE_military_control
			}
		}
		
		days_remove = 14
		priority = 2
		
		complete_effect = {
			SWE_increase_riksdag_rigidity = yes
			custom_effect_tooltip = adjust_swe_economic_tt
			add_to_variable = {
				tooltip = consumer_goods_factor_tt
				var = SWE_consumer_goods
				value = 0.05
			}
			add_to_variable = {
				tooltip = stability_factor_tt
				var = SWE_eco_stability
				value = -0.05
			}
			add_to_variable = {
				tooltip = production_speed_buildings_factor_tt
				var = SWE_build_speed
				value = 0.05
			}
		}
		
		remove_effect = {
			add_stability = 0.05
			add_political_power = 75
			remove_ideas = SWE_protecting_interests_of_businessowners
		}
		
		ai_will_do = {
			base = 1
			modifier = {
				factor = 0
				OR = {
					has_political_power > 75
					has_stability < 0.3
				}
			}
		}
	}
	SWE_increase_border_control_against_emigration = {
		allowed = { original_tag = SWE }
		visible = {
			NOT = { has_active_mission = SWE_emigrationsutredningen }
			has_dynamic_modifier = { modifier = SWE_emigration_problem }
		}
		available = {
			OR = {
				has_country_leader_ideology = conservatism
				has_country_leader_ideology = liberal_conservatism
				has_government = neutrality
				has_government = fascism
			}
			SWE_has_majority_for_reform_conservative = yes	# Maybe change to reactionary later
		}
		
		fire_only_once = yes
		
		cost = 50
		days_remove = 500
		
		complete_effect = {
			SWE_increase_riksdag_rigidity = yes
		}
		remove_effect = {
			custom_effect_tooltip = adjust_emigration_problem_tt
			add_to_variable = {
				tooltip = monthly_population_tt
				var = SWE_emigration_monthly_population
				value = 0.06
			}
			add_to_variable = {
				tooltip = stability_factor_tt
				var = SWE_emigration_stability_factor
				value = -0.03
			}
		}
		
		ai_will_do = {
			base = 0.5
			modifier = {
				add = 0.5
				OR = {
					has_completed_focus = SWE_trygger_government
					has_completed_focus = SWE_kungliga_regeringen
					has_completed_focus = SWE_military_control
				}
			}
			modifier = {
				factor = 2
				has_completed_focus = SWE_strengthen_border_control
			}
			modifier = {
				factor = 2
				is_historical_focus_on = no
			}
		}
	}
}
# Agrarian
SWE_riksdagens_decisions = {
	SWE_funds_to_bonderna = {
		allowed = {
			original_tag = SWE
		}
		available = {
			has_completed_focus = SWE_support_for_agrarian_sector
			NOT = { has_country_flag = riksdag_decisions_are_blocked }
			SWE_has_majority_for_reform_agrarian = yes
		}
		
		fire_only_once = yes
		visible = {
			NOT = { has_country_flag = unlock_agrarian_decisions }
			has_completed_focus = SWE_support_for_agrarian_sector
		}
		
		cost = 50
		days_remove = 70
		
		modifier = {
			consumer_goods_factor = 0.05
		}
		
		remove_effect = {
			custom_effect_tooltip = adjust_swe_economic_tt
			add_to_variable = {
				tooltip = stability_factor_tt
				var = SWE_eco_stability
				value = 0.05
			}
			add_to_variable = {
				tooltip = production_speed_industrial_complex_factor_tt
				var = SWE_civ_build_speed
				value = 0.05
			}
			add_to_variable = {
				tooltip = consumer_goods_factor_tt
				var = SWE_consumer_goods
				value = 0.05
			}
			set_country_flag = unlock_agrarian_decisions
			unlock_decision_tooltip = SWE_northern_agrarian_investment
			unlock_decision_tooltip = SWE_southern_agrarian_investment
		}
		
		ai_will_do = {
			base = 0.1
			modifier = {
				add = 0.1
				date > 1912.1.1
			}
			modifier = {
				add = 0.1
				date > 1912.6.1
			}
			modifier = {
				add = 0.1
				date > 1913.1.1
			}
			modifier = {
				add = 0.1
				date > 1913.6.1
			}
			modifier = {
				factor = 2
				has_stability < 0.6
			}
		}
	}
	SWE_northern_agrarian_investment = {
		allowed = {
			tag = SWE
		}
		
		available = {
			custom_trigger_tooltip = {
				tooltip = any_northern_sweden_state_can_build_infrastructure
				OR = {
					38 = {
						is_fully_controlled_by = ROOT
						free_building_slots = {
							building = infrastructure
							size > 0
						}
					}
					666 = {
						is_fully_controlled_by = ROOT
						free_building_slots = {
							building = infrastructure
							size > 0
						}
					}
				}
			}
			NOT = { has_decision = SWE_southern_agrarian_investment }
			SWE_has_majority_for_reform_agrarian = yes
			NOT = { has_country_flag = riksdag_decisions_are_blocked }
		}
		
		fire_only_once = yes
		visible = {
			has_country_flag = unlock_agrarian_decisions
			OR = {
				AND = {
					owns_state = 666
					666 = {
						free_building_slots = {
							building = infrastructure
							size > 0
						}
					}
				}
				AND = {
					owns_state = 38
					38 = {
						free_building_slots = {
							building = infrastructure
							size > 0
						}
					}
				}
			}	
		}
		
		cost = 25
		days_remove = 300
		
		modifier = {
			consumer_goods_factor = 0.05
		}
		
		remove_effect = {
			add_political_power = 10
			add_stability = 0.02
			if = {
				limit = {
					owns_state = 38
					38 = {
						free_building_slots = {
							building = infrastructure
							size > 0
						}
					}
				}
				38 = {
					add_building_construction = {
						type = infrastructure
						level = 1
						instant_build = yes
					}
				}
			}
			if = {
				limit = {
					owns_state = 666
					666 = {
						free_building_slots = {
							building = infrastructure
							size > 0
						}
					}
				}
				666 = {
					add_building_construction = {
						type = infrastructure
						level = 1
						instant_build = yes
					}
				}
			}
		}
		ai_will_do = {
			base = 1
		}
	}
	SWE_southern_agrarian_investment = {
		allowed = {
			tag = SWE
		}
		
		available = {
			custom_trigger_tooltip = {
				tooltip = any_southern_sweden_state_can_build_infrastructure
				OR = {
					138 = {
						is_fully_controlled_by = ROOT
						free_building_slots = {
							building = infrastructure
							size > 0
						}
					}
					139 = {
						is_fully_controlled_by = ROOT
						free_building_slots = {
							building = infrastructure
							size > 0
						}
					}
					140 = {
						is_fully_controlled_by = ROOT
						free_building_slots = {
							building = infrastructure
							size > 0
						}
					}
				}
			}
			NOT = { has_decision = SWE_northern_agrarian_investment }
			SWE_has_majority_for_reform_agrarian = yes
			NOT = { has_country_flag = riksdag_decisions_are_blocked }
		}
		
		fire_only_once = yes
		visible = {
			has_country_flag = unlock_agrarian_decisions
			OR = {
				AND = {
					owns_state = 138
					138 = {
						free_building_slots = {
							building = infrastructure
							size > 0
						}
					}
				}
				AND = {
					owns_state = 139
					139 = {
						free_building_slots = {
							building = infrastructure
							size > 0
						}
					}
				}
				AND = {
					owns_state = 140
					140 = {
						free_building_slots = {
							building = infrastructure
							size > 0
						}
					}
				}
			}	
		}
		
		cost = 25
		days_remove = 300
		
		modifier = {
			consumer_goods_factor = 0.05
		}
		
		remove_effect = {
			add_political_power = 10
			add_stability = 0.02
			if = {
				limit = {
					owns_state = 138
					138 = {
						free_building_slots = {
							building = infrastructure
							size > 0
						}
					}
				}
				138 = {
					add_building_construction = {
						type = infrastructure
						level = 1
						instant_build = yes
					}
				}
			}
			if = {
				limit = {
					owns_state = 139
					139 = {
						free_building_slots = {
							building = infrastructure
							size > 0
						}
					}
				}
				139 = {
					add_building_construction = {
						type = infrastructure
						level = 1
						instant_build = yes
					}
				}
			}
			if = {
				limit = {
					owns_state = 140
					140 = {
						free_building_slots = {
							building = infrastructure
							size > 0
						}
					}
				}
				140 = {
					add_building_construction = {
						type = infrastructure
						level = 1
						instant_build = yes
					}
				}
			}
		}
		ai_will_do = {
			base = 1
		}
	}
	SWE_reduce_jordvardeskatten = {
		# https://snd.se/sv/vivill/party/c/p/1919
		# Reduced land tax
		allowed = {
			original_tag = SWE
		}
		available = {
			# Is agrarian but want some opposition to this
			SWE_has_majority_for_reform_conservative = yes
		}
		
		fire_only_once = yes
		visible = {
			OR = {
				has_completed_focus = SWE_pursue_agrarian_policies
				has_completed_focus = SWE_win_over_agrarian_parties
			}
		}
		
		cost = 50
		days_remove = 100
		
		remove_effect = {
			add_to_variable = {
				tooltip = swe_con_neu_vote_a_tt
				var = SWE_con_neu_votes_a
				value = -0.01
			}
			add_to_variable = {
				tooltip = swe_agr_neu_vote_a_tt
				var = SWE_agr_neu_votes_a
				value = 0.01
			}
			custom_effect_tooltip = skip_line
			custom_effect_tooltip = adjust_swe_economic_tt
			add_to_variable = {
				tooltip = stability_factor_tt
				var = SWE_eco_stability
				value = 0.05
			}
			add_to_variable = {
				tooltip = consumer_goods_factor_tt
				var = SWE_consumer_goods
				value = 0.05
			}
		}
		
		ai_will_do = {
			base = 0.1
		}
	}
	SWE_agrarian_protection_reforms = {
		# Reform to protect people's right to land
		allowed = {
			original_tag = SWE
		}
		available = {
			SWE_has_majority_for_reform_agrarian = yes
		}
		
		fire_only_once = yes
		visible = {
			OR = {
				has_completed_focus = SWE_pursue_agrarian_policies
				has_completed_focus = SWE_win_over_agrarian_parties
			}
		}
		
		cost = 50
		days_remove = 100
		
		remove_effect = {
			if = {
				limit = {
					NOT = { has_idea = free_trade }
				}
				add_ideas = free_trade
			}
			custom_effect_tooltip = adjust_swe_political_tt
			add_to_variable = {
				tooltip = trade_laws_cost_factor_tt
				var = SWE_pol_trade_laws_cost_factor
				value = 0.2
			}
			custom_effect_tooltip = adjust_swe_economic_tt
			add_to_variable = {
				tooltip = stability_factor_tt
				var = SWE_eco_stability
				value = 0.05
			}
			custom_effect_tooltip = skip_line
			add_to_variable = {
				tooltip = swe_con_dem_vote_a_tt
				var = SWE_con_dem_votes_a
				value = -0.01
			}
			add_to_variable = {
				tooltip = swe_lib_dem_vote_a_tt
				var = SWE_lib_dem_votes_a
				value = -0.01
			}
			add_to_variable = {
				tooltip = swe_agr_dem_vote_a_tt
				var = SWE_agr_dem_votes_a
				value = 0.02
			}
		}
		
		ai_will_do = {
			base = 1
		}
	}
	SWE_anti_trust_and_monopoly_reforms_agr = {
		# https://snd.se/sv/vivill/party/c/v/1917
		# Anti-monopoly stuff that may hurt farmers
		allowed = {
			tag = SWE
		}
		visible = {
			OR = {
				has_completed_focus = SWE_pursue_agrarian_policies
				has_completed_focus = SWE_win_over_agrarian_parties
			}
		}
		available = {
			SWE_has_majority_for_reform_agrarian = yes
		}
		fire_only_once = yes
		
		days_remove = 365
		cost = 100
		
		complete_effect = {
			SWE_increase_riksdag_rigidity = yes
		}
		remove_effect = {
			custom_effect_tooltip = adjust_swe_economic_tt
			add_to_variable = {
				tooltip = political_power_cost_tt
				var = SWE_eco_political_power_cost
				value = -0.05
			}
			add_to_variable = {
				tooltip = stability_factor_tt
				var = SWE_eco_stability
				value = 0.05
			}
		}
		
		ai_will_do = {
			base = 1
		}
	}
	# Look up  more
}
# Riksdag - "Normal" Reforms
SWE_riksdagens_decisions = {
	SWE_neutralitetsregler = {
		# Let's work on our neutrality a bit (thanks to Albert Ehrensvärd)
		# Some sources found states it is essentially Haag-convention? I need to find old document
		# https://www.regeringen.se/contentassets/357fe4e9219a4e61ab14614d0a95c11d/deklarationer-vaxlade-med-danmark-och-norge-rorande-likartade-neutralitetsbestammelserso_1912_11.pdf
		allowed = {
			tag = SWE
		}
		
		available = {
			has_government = democratic
			OR = {
				has_idea = SWE_albert_ehrensvard
				threat > 0.10
			}
			focus_progress = {
				focus = SWE_pro_entente_stance
				progress < 0.01
			}
			focus_progress = {
				focus = SWE_rally_to_neutrality
				progress < 0.01
			}
			focus_progress = {
				focus = SWE_strengthen_german_relations
				progress < 0.01
			}
		}
		visible = {
			date > 1911.9.11
			NOT = {
				has_completed_focus = SWE_pro_entente_stance
				has_completed_focus = SWE_rally_to_neutrality
				has_completed_focus = SWE_strengthen_german_relations
			}
		}
		
		fire_only_once = yes
		
		days_remove = 25
		cost = 25
				
		remove_effect = {
			add_stability = 0.05
			add_political_power = 75
			add_ideas = SWE_neutralitetsforklaring
			custom_effect_tooltip = neutralitetsforklaring_removal_tt
		}
		
		ai_will_do = {
			base = 1
			modifier = {
				factor = 100
				date > 1911.11.26
			}
		}
	}
	SWE_merge_folkskola_and_smaskola = {
		# Make sure people can study for 6 years in a more organised way rather than in parallel
		allowed = {
			original_tag = SWE
		}
		visible = {
			date > 1917.6.1
		}
		available = {
			has_capitulated = no
		}
		fire_only_once = yes
		
		modifier = { consumer_goods_factor = 0.05 }
		
		days_remove = 365
		cost = 50
		
		remove_effect = {
			add_ideas = SWE_school_reform
			if = {
				limit = { date > 1918.1.1 }
				unlock_decision_tooltip = SWE_higher_learning_for_tekniska_skolor
			}
			if = {
				limit = { date > 1919.1.1 }
				unlock_decision_tooltip = SWE_upl_1919
			}
		}
		
		ai_will_do = {
			base = 0
			modifier = {
				add = 100
				date > 1918.1.1
			}
		}
	}
	SWE_upl_1919 = {
		# UPL 1919
		# Basically set a proper framework for the education
		allowed = {
			original_tag = SWE
		}
		visible = {
			date > 1919.1.1
			OR = {
				has_idea = SWE_school_reform
				has_idea = SWE_school_reform_el
			}
		}
		available = {
			has_civil_war = no
		}
		
		cost = 75
		
		days_remove = 14
		fire_only_once = yes
		
		remove_effect = {
			if = {
				limit = { has_idea = SWE_school_reform }
				swap_ideas = {
					add_idea = SWE_school_reform_19
					remove_idea = SWE_school_reform
				}
			}
			if = {
				limit = { has_idea = SWE_school_reform_el }
				swap_ideas = {
					add_idea = SWE_school_reform_best
					remove_idea = SWE_school_reform_el
				}
			}
		}
		
		ai_will_do = {
			base = 0
			modifier = {
				add = 50
				date > 1919.10.1
			}
			modifier = {
				add = 50
				date > 1919.10.16
			}
		}
	}
	SWE_higher_learning_for_tekniska_skolor = {
		allowed = {
			original_tag = SWE
		}
		visible = {
			date > 1918.1.1
			OR = {
				has_idea = SWE_school_reform
				has_idea = SWE_school_reform_19
			}
		}
		available = {
			has_capitulated = no
			NOT = { has_country_flag = riksdag_decisions_are_blocked }
		}
		fire_only_once = yes
		
		days_remove = 400
		cost = 50
		modifier = {
			consumer_goods_factor = 0.03
		}
		
		remove_effect = {
			add_tech_bonus = {
				name = mechanical_innovation
				category = electronics
				category = industry
				uses = 1
				bonus = 0.25
			}
			if = {
				limit = { has_idea = SWE_school_reform }
				swap_ideas = {
					add_idea = SWE_school_reform_el
					remove_idea = SWE_school_reform
				}
			}
			if = {
				limit = { has_idea = SWE_school_reform_19 }
				swap_ideas = {
					add_idea = SWE_school_reform_best
					remove_idea = SWE_school_reform_19
				}
			}
		}
		
		ai_will_do = {
			base = 100
		}
	}
	SWE_allow_usage_of_more_mineralrich_land = {
		# https://www.riksdagen.se/sv/dokument-och-lagar/dokument/proposition/kungl.-majts-nad.-proposition-no-173_d730173
		# From understanding, making it easier to make use of land
		allowed = {
			original_tag = SWE
		}
		available = {
			NOT = { has_country_flag = riksdag_decisions_are_blocked }
		}
		fire_only_once = yes
		
		days_remove = 120
		cost = 25
		
		remove_effect = {
			add_tech_bonus = {
				name = excavation_tech
				category = excavation_tech
				uses = 1
				bonus = 0.2
			}
		}
		
		ai_will_do = {
			base = 100
		}
	}
	SWE_limit_foreign_ownership_of_natural_resources = {
		# https://www.econstor.eu/bitstream/10419/56257/1/361985312.pdf
		# "Restrictions for foreign owners were further extended in 1916 when new legislation was passed that prevented foreign owners from having more than 20 percent of the voting rights in Swedish corporations owning natural resources. This law mainly hit firms in mining, forestry, pulp and paper, and iron and steel"
		allowed = {
			tag = SWE
		}
		visible = {
			date > 1916.1.1	# Make it part of focus later
		}
		
		fire_only_once = yes
		
		days_remove = 365
		cost = 50
		
		remove_effect = {
			custom_effect_tooltip = adjust_swe_political_tt
			add_to_variable = {
				tooltip = drift_defence_factor_tt
				var = SWE_pol_drift_defence_factor
				value = 0.10
			}
			custom_effect_tooltip = adjust_swe_economic_tt
			add_to_variable = {
				tooltip = political_power_cost_tt
				var = SWE_eco_political_power_cost
				value = -0.05
			}
		}
		
		ai_will_do = {
			base = 1
		}
	}
	SWE_implement_laws_around_driving_license = {
		# Körkort
			# A few laws around körkort was implemented
		allowed = {
			tag = SWE
		}
		visible = {
			date > 1916.1.1
			NOT = {
				has_country_flag = { 
					flag = sweden_korkort_flag
					value = 1
				}
			}
		}
		
		fire_only_once = yes
		
		days_remove = 120
		cost = 20
		
		remove_effect = {
			add_stability = 0.02
			if = {
				limit = {
					has_country_flag = sweden_korkort_flag
				}
				modify_country_flag = {
					flag = sweden_korkort_flag
					value = 1
				}
			}
			else = {
				set_country_flag = {
					flag = sweden_korkort_flag
					value = 1
				}
			}
		}
		ai_will_do = {
			base = 10
		}
	}
	SWE_implement_laws_around_driving_license_2 = {
		# Körkort
			# Some tests were implemented to test ability to drive for drivings license (theoretical and practical)
		allowed = {
			tag = SWE
		}
		visible = {
			date > 1920.1.1
			has_country_flag = {
				flag = sweden_korkort_flag
				days > 365
			}
			has_country_flag = { 
				flag = sweden_korkort_flag
				value = 1
			}
			NOT = {
				has_country_flag = { 
					flag = sweden_korkort_flag
					value = 3
				}
			}
		}
		
		fire_only_once = yes
		
		days_remove = 120
		cost = 15
		
		remove_effect = {
			add_stability = 0.015
		}
		ai_will_do = {
			base = 10
		}
	}
	SWE_implement_laws_around_driving_license_3 = {
		# Körkort
			# 1923 ökade kraven på föraren, så att man både skulle vara en god förare och vara väl förtrogen med fordonet.
		allowed = {
			tag = SWE
		}
		visible = {
			date > 1923.1.1
			has_country_flag = {
				flag = sweden_korkort_flag
				days > 365
			}
			has_country_flag = { 
				flag = sweden_korkort_flag
				value = 2
			}
			NOT = {
				has_country_flag = { 
					flag = sweden_korkort_flag
					value = 3
				}
			}
		}
		
		fire_only_once = yes
		
		days_remove = 120
		cost = 10
		
		remove_effect = {
			add_stability = 0.01
			if = {
				limit = {
					has_country_flag = sweden_korkort_flag
				}
				modify_country_flag = {
					flag = sweden_korkort_flag
					value = 1
				}
			}
			else = {
				set_country_flag = {
					flag = sweden_korkort_flag
					value = 1
				}
			}
		}
		ai_will_do = {
			base = 10
		}
	}
}
# Some Sapmi stuff
SWE_riksdagens_decisions = {
	SWE_establish_nomadskola = {
		# Nomadskola
		# https://sv.wikipedia.org/wiki/Sameskola
			# School used to help convert samer
			# Really based on racism
			# Logistics didn't work out generally
		allowed = {
			original_tag = SWE
		}
		visible = {
			date > 1913.1.1
			has_idea = SWE_visteskola
		}
		
		fire_only_once = yes
		
		days_remove = 14
		cost = 15
		
		remove_effect = {
			swap_ideas = {
				add_idea = SWE_nomadskola
				remove_idea = SWE_visteskola
			}
			if = {
				limit = { has_government = fascism }
				add_popularity = {
					ideology = fascism
					popularity = 0.02
				}
			}
			else = {
				add_popularity = {
					ideology = ROOT
					popularity = 0.01
				}
				add_popularity = {
					ideology = fascism
					popularity = 0.01
				}
			}
		}
		
		ai_will_do = {
			base = 10
		}
	}
	SWE_renbeteskonventionen_1919 = {
		# Renbeteskonventionen 1919
			# Need event about the discussion around it in 1913
				# If Sweden goes through with it, this decision should be available
				# If not, then no
		allowed = {
			original_tag = SWE
		}
		visible = {
			has_completed_focus = SWE_renbeteskonventionen
		}
		available = {
			#date > 1919.1.1
			controls_state = 666
		}
		fire_only_once = yes
		
		days_remove = 35
		cost = 15
		
		complete_effect = {
			add_stability = -0.01
		}
		
		remove_effect = {
			if = {
				limit = { has_government = fascism }
				add_popularity = {
					ideology = fascism
					popularity = 0.02
				}
			}
			else = {
				add_popularity = {
					ideology = ROOT
					popularity = 0.01
				}
				add_popularity = {
					ideology = fascism
					popularity = 0.01
				}
			}
			666 = {
				# Stealing their land
				add_extra_state_shared_building_slots = 1
			}
			# Some small-ish manpower loss possibly?
		}
		
		ai_will_do = {
			base = 10
			modifier = {
				factor = 0
				date < 1919.1.1
			}
		}
	}
	SWE_fasta_nomadskola = {
		# Discussions around ending them happened around this time
			# Although people were pretty racist back then so it was less so "oh we shouldn't treat them lesser" and moreso "it is inconvient to have a moving school"
			# So they made a stationary separate school for sapmi
			# This makes it easier to upkeep...instead of just having one school <-<
		allowed = {
			original_tag = SWE
		}
		visible = {
			date > 1918.1.1
			OR = {
				has_idea = SWE_visteskola
				has_idea = SWE_nomadskola
			}
		}
		available = {
			date > 1920.1.1
			fascism < 0.1
			NOT = { has_government = fascism }
			NOT = { has_country_flag = riksdag_decisions_are_blocked }
			NOT = { has_decision = SWE_establish_nomadskola }
		}
		
		fire_only_once = yes
		
		days_remove = 60
		cost = 75
		
		remove_effect = {
			add_stability = -0.005
			if = {
				limit = { has_idea = SWE_nomadskola }
				swap_ideas = {
					add_idea = SWE_nomadskola_2
					remove_idea = SWE_nomadskola
				}
			}
			if = {
				limit = { has_idea = SWE_visteskola }
				swap_ideas = {
					add_idea = SWE_nomadskola_2
					remove_idea = SWE_visteskola
				}
			}
		}
		
		ai_will_do = {
			base = 0
			modifier = {	# If for whatever reason you play until this point
				add = 1
				date > 1935.1.1
			}
		}
	}
}
