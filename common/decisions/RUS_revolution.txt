#REVOLUTION
TGWR_great_power_politics = {
	RUS_revolution_conditions = {
		icon = eng_support_imperialist_coup
		allowed = { tag = RUS }
		available = {
			hidden_trigger = {
				always = no
			}
			custom_trigger_tooltip = {
				tooltip = RUS_revolution_war_tt
				has_war_with_major = yes
				OR = {
					has_idea = RUS_famine_in_cities
					AND = {
						any_country = {
							is_major = yes
							war_length_with = { 
								tag = ROOT 
								months > 16
							}
						}
						OR = {
							casualties > 3000000
							AND = {
								OR = {
									AND = {
										POL = {
											exists = yes
										}
										surrender_progress > 0.04
									}
									surrender_progress > 0.1
								}
								casualties > 2500000
							}
							AND = {
								OR = {
									AND = {
										POL = {
											exists = yes
										}
										surrender_progress > 0.1
									}
									surrender_progress > 0.2
								}
								casualties > 2000000
							}
						}
					}
					AND = {
						any_country = {
							is_major = yes
							war_length_with = { 
								tag = ROOT 
								months > 28 
							}
						}
						OR = {
							has_idea = RUS_food_shortages
							casualties > 2000000
							AND = {
								OR = {
									AND = {
										POL = {
											exists = yes
										}
										surrender_progress > 0.04
									}
									surrender_progress > 0.1
								}
								casualties > 1500000
							}
							AND = {
								OR = {
									AND = {
										POL = {
											exists = yes
										}
										surrender_progress > 0.1
									}
									surrender_progress > 0.2
								}
								casualties > 1000000
							}
						}
					}
				}
			}
			custom_trigger_tooltip = {
				tooltip = RUS_revolution_avoided_tt
				has_agrarian_question = no
				has_labour_question = no
			}
			has_agrarian_question = no
			has_labour_question = no
		}
		visible = {
			has_active_mission = RUS_whirlwinds_of_danger_mission
		}
		custom_cost_trigger = {
			always = no
		}
		custom_cost_text = decision_cost_INFO
		complete_effect = {
		}
	}
	RUS_whirlwinds_of_danger_mission = {
		icon = eng_support_imperialist_coup
		allowed = {
			always = no
		}
		activation = {
			always = no
		}
		available = {
			hidden_trigger = {
				has_agrarian_question = no
				has_labour_question = no
			}
		}
		cancel_trigger = {
			hidden_trigger = {
				has_war_with_major = yes
				OR = {
					has_idea = RUS_famine_in_cities
					AND = {
						any_country = {
							is_major = yes
							war_length_with = { 
								tag = ROOT 
								months > 16
							}
						}
						OR = {
							casualties > 3000000
							AND = {
								OR = {
									AND = {
										POL = {
											exists = yes
										}
										surrender_progress > 0.04
									}
									surrender_progress > 0.1
								}
								casualties > 2500000
							}
							AND = {
								OR = {
									AND = {
										POL = {
											exists = yes
										}
										surrender_progress > 0.1
									}
									surrender_progress > 0.2
								}
								casualties > 2000000
							}
						}
					}
					AND = {
						any_country = {
							is_major = yes
							war_length_with = { 
								tag = ROOT 
								months > 28 
							}
						}
						OR = {
							has_idea = RUS_food_shortages
							casualties > 2000000
							AND = {
								OR = {
									AND = {
										POL = {
											exists = yes
										}
										surrender_progress > 0.04
									}
									surrender_progress > 0.1
								}
								casualties > 1500000
							}
							AND = {
								OR = {
									AND = {
										POL = {
											exists = yes
										}
										surrender_progress > 0.1
									}
									surrender_progress > 0.2
								}
								casualties > 1000000
							}
						}
					}
				}
			}
		}
		days_mission_timeout = 2500
		fire_only_once = yes
		is_good = no
		timeout_effect = {
			custom_effect_tooltip = RUS_revolution_tt
			hidden_effect = {
				if = {
					limit = {
						has_war_with_major = yes
					}
					#FEBRUARY REVOLUTION
					if = {
						limit = {
							duma_exists = yes
							is_constitutional_monarchy = no
						}
						country_event = ww1_russia_revolution.1
						set_global_flag = february_revolution
					}
					#PETROGRAD COMMUNE
					else = {
						country_event = ww1_russia_revolution.1
						set_global_flag = petrograd_commune
					}
				}
				#NEW 1905
				else = {
					country_event = ww1_russia_revolution.1
					set_global_flag = new_1905
				}
			}
		}
		cancel_effect = {
			hidden_effect = {
				if = {
					limit = {
						duma_exists = yes
					}
					country_event = ww1_russia_revolution.1
					set_global_flag = february_revolution
				}
				else = {
					country_event = ww1_russia_revolution.1
					set_global_flag = petrograd_commune
				}
			}
		}
	}
}
#FEBRUARY REVOLUTION
TGWR_great_power_politics = {
}
#PETROGRAD COMMUNE
TGWR_great_power_politics = {
}
#REVOLUTION DURING PEACETIME/NEW 1905
TGWR_great_power_politics = {
}