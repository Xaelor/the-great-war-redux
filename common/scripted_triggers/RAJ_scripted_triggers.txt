is_raj_tag_tt = {
	OR = {
		original_tag = RIN
		original_tag = RAJ
	}
}
is_raj_tag = {
	custom_trigger_tooltip = {
		tooltip = is_raj_tag
		is_raj_tag_tt = yes
	}
}
AFG_decisions_visible = {
	if = {
		limit = {
			OR = {
				original_tag = GER
                original_tag = TUR
			}
		}
        NOT = {
            has_country_flag = great_war_loser
            is_in_faction_with = AFG
        }
		OR = {
			has_war_with = ENG
			has_war_with = RAJ
			#is_debug = yes
		}
		AFG = {
            NOT = {
                has_war_with = GER
            }
		}
	}
	if = {
		limit = {
			OR = {
				original_tag = AFG
				original_tag = ENG
			}
		}
		has_global_flag = GER_has_active_mission_in_kabul
		GER = {
		    has_war_with = ENG
		    has_war_with = RAJ
		}
	}
}